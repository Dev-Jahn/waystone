version: 1
project: waystone
milestones: []
# Optional task fields:
#   deps       — task ids this one depends on
#   milestone  — M<N>
#   round      — round id the task was worked/closed in
#   anchor     — §-anchor of the SSOT section the task governs
#   severity   — blocker | major | minor (review findings)
#   origin     — where the task came from (e.g. review-<round>)
#   branch     — working branch
#   notes      — free-form traceability (old codenames, context)
#   ruling     — the user's decision on a decision/... task
#   result     — a recorded measurement/outcome
#   scope      — repo-relative path-prefix list; maintain with `waystone task set <id> --scope-add`
#   lane       — {branch, base_sha, depends_on} for parallel worktree lanes (`waystone lanes verify`)
tasks:
  - id: fix/boundary-hook-cli-resolution
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-adopt-dogfooding
    result: "2차 위임 apply — plugin hooks.json Stop hook(비차단·마커 게이트), install hooks 마커 재설계+레거시 감지, 561 tests OK. 기록: .waystone/delegations/20260716T014050Z-fix-boundary-hook-cli-resolution"
    accept: ["plugin hooks.json에 Stop hook이 추가되어 ${CLAUDE_PLUGIN_ROOT} 경로로 boundary check를 실행하며, hook 경로 어디에도 bare waystone 호출이 남지 않는다", ".waystone.yml이 없거나 enable 마커가 없는 프로젝트에서는 Stop hook이 즉시 성공 종료로 no-op한다 (기존 waystone hook들의 no-op-fast 패턴)", "waystone install hooks는 .claude/settings.json을 쓰지 않고 consent 기록 + 프로젝트 상태 enable 마커로 동작하며, 마커 제거가 문서화된 롤백 경로다", "templates/waystone-boundary-hook.json이 제거되고 README, init 스킬, conventions, tests의 참조가 일관되게 갱신된다", "Codex host 반영 여부가 명시적으로 구현되거나 사유와 함께 기록된다 (무기록 드롭 금지)", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "Stop hook 스크립트는 launcher가 어떤 종료 코드를 반환해도 자신은 0으로 종료하고 stderr는 그대로 통과시킨다 (경고 비차단 계약 — blocking exit 전파 금지)", "waystone install hooks는 레거시 .claude/settings.json의 waystone Stop hook 설치를 감지하면 제거 안내를 출력한다 (사용자 설정 파일 자동 수정 금지)", "hooks.json의 모든 hook command에서 ${CLAUDE_PLUGIN_ROOT}를 따옴표로 감싼다", "init 스킬에서 Codex 저장 분리를 주장하는 서술을 0.9 통합 저장 사실에 맞게 바로잡는다"]
    scope: ["templates", "hooks", "scripts", "skills", "README.md"]
    notes: "owner 결정 (2026-07-16): boundary hook을 plugin 소유 hooks.json으로 이동 — CLAUDE_PLUGIN_ROOT 해석(공식 메커니즘, 버전 독립), 프로젝트별 enable 마커(.waystone/ 내)로 게이트, 기존 hook들의 no-op-fast 패턴 준수. install hooks는 consent+마커 기록으로 재설계(settings.json 미작성). 이 repo의 설치본 settings.json은 롤백 절차대로 삭제(활성 규칙 0개라 손실 없음). 근거: CC 문서상 bin PATH는 Bash tool 한정, settings.json hook의 버전 독립 참조 공식 경로 부재. 템플릿 waystone-boundary-hook.json은 제거 대상, README/init 스킬/tests 갱신 필요. Codex host 반영 여부는 구현 시 명시적으로 결정·기록(무기록 드롭 금지)."
    title: "install hooks가 설치한 boundary hook이 bare 'waystone'을 호출해 hook 실행 환경(PATH에 plugin bin 미주입)에서 command not found — 템플릿/설치 시점에 버전 독립적 CLI 경로 해석 필요"
    status: done
    severity: "major"
  - id: fix/verify-worktree-self-contamination
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-adopt-dogfooding
    result: "2차 위임 apply — WAYSTONE_VERIFIER_SESSION guard + RED-first 재현 fixture, 562 tests OK. 라이브 verify 복원은 다음 릴리스+plugin update 후. 기록: .waystone/delegations/20260716T074509Z-fix-verify-worktree-self-contamination"
    accept: ["delegate verify는 리뷰 worktree 안에 .waystone 프로젝트 상태(디렉터리 생성, profile 시딩, lock 파일)를 생성하거나 수정하지 않는다", ".waystone.yml이 커밋된 프로젝트와 레거시 시드가 존재하는 머신 조건을 재현한 fixture에서 verify가 tree-mutation postcondition을 통과하고 verify 아티팩트를 기록한다 (RED-first 재현 테스트)", "verifier가 실제로 worktree 파일을 생성·수정하는 진짜 오염은 여전히 postcondition이 거부한다 (기존 회귀 테스트 유지)", "어느 호출 경로가 worktree를 project root로 취급했는지 원인이 구현 보고에 명시된다 (증상 패치 금지)", "전체 테스트 스위트와 ruff F401,F841 검사가 통과한다"]
    scope: ["scripts"]
    notes: "두 번째 dogfooding finding (2026-07-16, 첫 위임 fix/boundary-hook-cli-resolution의 verify 2회 연속 실패). 증거: 1차 실패 사유 ignored_untracked+untracked_manifest, 2차 untracked_manifest; worktree/.waystone/{.gitignore,profile.yml(시딩, mtime 보존),lock(2차 시각)} 생성 확인. 원인 후보: verify 경로의 load_config/ensure_project_state_dir/migrate/hold_lock가 worktree를 project root로 취급. 수정 방향 후보: verify가 worktree를 프로젝트로 취급하지 않도록 root 고정, 또는 postcondition이 하네스 자신의 상태 디렉터리를 제외(단 verifier 오염과 구별 필요 — 안이한 예외는 금물). 검증: .waystone.yml 커밋된 fixture + 시드 존재 조건 재현 테스트."
    title: "delegate verify가 리뷰 worktree 안에 .waystone 프로젝트 상태(profile 시딩·lock)를 생성해 자신의 tree-mutation postcondition에 걸림 — .waystone.yml이 커밋된 프로젝트 + 레거시 시드 존재 머신 조합에서 verify가 결정론적으로 실패"
    status: done
    severity: "major"
  - id: chore/verifier-session-guard-hardening
    scope: ["scripts", "hooks"]
    notes: "출처: fix/verify-worktree-self-contamination 2차 위임의 raw codex 적대 리뷰 (scratchpad/adv-review-verify-fix.md, blocker 0·major 4·minor 1). main-session 판정: boundary marker는 worktree 도달 불가(마커가 git 밖 .waystone/에만 존재), uv-cache/broker는 미재현 edge, RUN 시딩은 무결성 무해 — 어느 것도 이번 인수를 막지 않으나 가드 커버리지 일반화 + env 수명 명시 scoping + RUN 경계 기록이 남은 일."
    title: "verifier session guard 경화: boundary_check/tasks_read_nudge 등 미가드 hook 표면, companion broker의 guard env 수명 범위, RUN 단계 구현자 세션의 .waystone 시딩(무해하나 무기록) — 적대 리뷰 major 4건의 후속 처리"
    status: "dropped"
    severity: "minor"
  - id: docs/adopt-waystone-harness
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-adopt-dogfooding
    result: "SSOT.md + .waystone.yml + ADR-0000 + docs 구조 + release EXCLUDES 확장 + profile 6-role binding. main 누출 0을 tree-hash 시뮬레이션으로 증명 (커밋 4f8ddbc·2d318c7)"
    title: "waystone 자기채택 bootstrap: SSOT.md 합성(ideate), init(패킷 리뷰·warn-allowed·delegation on), ADR-0000, 부산물 dev-only 릴리스 제외(EXCLUDES), 3축 profile 구성"
    status: done
  - id: fix/round-packet-remote-visibility
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "2차 위임 apply(594b8ad, 사용자 waiver 1건) — publication 게이트(실 bare-remote RED-first), bind 단계 분리, 엄격 Reviewing 파서, corrupt loud. edge 우회 5건은 fix/publication-gate-bypasses로 이관. 기록: .waystone/delegations/20260716T101557Z-fix-round-packet-remote-visibility"
    accept: ["round skill의 packet 흐름이 request 작성(정확한 Reviewing 줄 형식 명시) → 커밋 → push → remote verify → 보고 순서로 재규정되고, 전달 안내가 remote에서 repo를 읽는 리뷰어 기준으로 서술된다", "round close의 binding target_sha와 packet Reviewing sha의 의미론이 일원화되어 문서와 코드가 일치한다", "ingest에서 packet Reviewing 줄 파싱 실패가 조용히 스킵되지 않고 stderr 경고를 낸다", "전체 테스트 스위트(신규·갱신 계약 테스트 포함)와 ruff F401,F841 검사가 통과한다", "round의 publication 게이트가 push된 HEAD 트리에 해당 round의 request 파일과 binding이 존재함을 확인한다 — 미추적·부분 커밋 상태로는 통과하지 않는다", "Reviewing 줄 파싱은 개행이나 가변 공백을 허용하지 않는 정확한 단일 줄 형식만 수용한다", "malformed 또는 corrupt binding·Reviewing은 improve 투영을 포함한 모든 소비 지점에서 조용히 넘어가지 않고 경고 또는 fail-loud로 드러난다 (corrupt 사이드카 포함)", "publication 계약이 실제 temp remote를 사용한 subprocess 수준 테스트로 RED-first 검증된다"]
    scope: ["skills", "scripts"]
    notes: "세 번째 dogfooding finding (2026-07-16, 첫 round 마감에서 사용자 적발). 부수 지점 1: round close가 찍는 binding target_sha(close 시점 HEAD)와 packet의 Reviewing(closeout 커밋)이 한 커밋 어긋남 — close→closeout→request의 sha 의미론 일원화 필요. 부수 지점 2 (ingest에서 확인): packet의 'Reviewing:' 줄에 부연 텍스트를 붙이면 parse_packet_request_binding 정규식이 조용히 실패해 binding 갱신이 무기록 스킵됨 — 형식 이탈 시 stderr 경고 필요 + skill에 정확한 줄 형식 명시. skill Step 4의 순서를 'request 작성(Reviewing=closeout sha) → 커밋 → push → remote verify → 보고'로 재규정 + 전달 안내를 remote 경로 기준으로. 대상: skills/round/SKILL.md, scripts/review.py."
    title: "round skill packet 모드: 리뷰 요청 순서가 '작성→커밋/푸시→보고'가 아니라 'closeout 푸시→작성→로컬 전달'이라 remote에서 repo를 읽는 리뷰어에게 packet이 안 보임 — 요청 파일을 커밋·푸시 후 보고하도록 skill 순서 수정"
    status: done
    severity: "major"
  - id: feat/reviewer-self-declared-identity
    accept: ["review-request 템플릿에 리뷰어가 회신 첫머리에 자신의 모델 slug와 effort를 명시하도록 요구하는 한 줄이 추가된다", "review ingest가 회신 첫머리의 자기선언을 파싱해 reviewer identity로 기록하고, 선언이 없으면 추측 없이 unknown으로 기록하며 stderr 경고를 낸다", "자기선언 identity와 reviewer_configured 판정(review-skipped-closes-v1) 및 request binding reviewers 필드의 관계가 재정의되어 문서와 코드가 일치한다 — profile reviewer binding의 잔여 역할도 명시된다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "자기선언이 bind된 route 또는 구성된 리터럴 reviewer와 일치할 때만 reviewer_configured=True로 기록되고, 선언 부재나 불일치는 guard(review-skipped-closes-v1)의 configured-feedback으로 인정되지 않으며 경고를 남긴다 (guard 약화 금지)", "강제 재ingest(--force)는 feedback 파일과 canonical overlay 이벤트의 reviewer identity를 일관되게 갱신한다 (모순 기록 금지, identity 정정 경로 테스트 포함)", "feedback 이벤트는 자신이 소비한 request binding의 결속 증거(대상 sha 또는 digest)를 기록해 round 재준비·재사용 시 회신-binding 불일치를 탐지할 수 있다", "improve의 reviewer 관련 투영이 자기선언 identity와 effort를 소비한다", "invalid UTF-8 등 손상된 선언은 추측 없이 unknown으로 기록되고 경고를 남긴다 (대체문자 identity 금지)", "선언 위치 규칙(첫 비어있지 않은 줄)이 파서, round 스킬, review 스킬, 템플릿 네 표면에서 동일하게 서술되고 템플릿 예시가 파서를 실제로 통과하는 round-trip 테스트가 있다"]
    scope: ["templates", "scripts", "skills"]
    notes: "사용자 지시 (2026-07-16): 외부 리뷰 모델은 언제든 바뀌므로 하드코딩은 부적합. 설계 시 결정할 것: (a) 선언 부재 시 정직한 unknown 처리(추측 금지), (b) reviewer_configured 판정(review-skipped-closes-v1의 configured-reviewer check) 및 request binding의 reviewers 필드와의 관계 재정의, (c) profile reviewer binding의 잔여 역할(기본값? 폐기?). 대상: templates/review-request.md, scripts/review.py(ingest 파싱), skills/round·review, 관련 계약 테스트. 실증 (2026-07-16 첫 ingest): 실제 리뷰어는 gpt-5.6-sol pro effort였으나 profile의 낡은 binding(gpt-5.5-pro)이 그대로 기록되어 사용자가 3곳(feedback 헤더·binding.json·profile) 수동 정정 — 하드코딩 identity의 실패 사례."
    title: "외부 리뷰어 identity를 config/profile 하드코딩(예: chatgpt:gpt-5.5-pro) 대신 리뷰 회신의 자기선언으로 수집 — 리뷰 패킷 템플릿에 '리뷰 첫머리에 본인의 모델 slug(effort 포함)를 명시해 달라' 한 줄 추가하고 ingest가 이를 파싱해 기록"
    status: "dropped"
  - id: feat/effort-pro-ultra
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "위임 apply — enum 확장(schema=runtime), ultra argv 계약, pro 실행형 거부, verifier effort 복원(codex-cli). companion 잔여는 fix/companion-verifier-effort-forwarding. 기록: .waystone/delegations/20260716T104721Z-feat-effort-pro-ultra"
    accept: ["effort 어휘에 pro와 ultra가 추가된다 (templates/profile-schema.json과 delegate.py의 effort 검증이 일치)", "effort 값별 실행 가능 경로가 매핑된다: ultra는 codex CLI에 model_reasoning_effort로 전달되고, pro는 실행형(external-runner) 경로에서 fail-loud로 거부되며 웹 리뷰 경로 전용임이 문서화된다 (무기록 대체 금지)", "README와 conventions의 effort 서술이 갱신되어 docs gate를 통과한다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    scope: ["templates", "scripts"]
    notes: "사용자 지시 (2026-07-16): 'pro'는 gpt-5.5까지 model variant였으나 5.6부터 effort로 취급되는 것으로 보임. 현 enum none|minimal|low|medium|high|xhigh (profile-schema.json + delegate.py _EFFORT_VALUES). 설계 시 결정할 것: effort 값별 실행 가능 경로 매핑 — pro는 web ChatGPT 경로 전용(CLI 전달 불가), ultra는 codex CLI 경로(model_reasoning_effort) — 실행 불가 조합은 fail-loud로 거부(무기록 대체 금지). 대상: templates/profile-schema.json, scripts/delegate.py(검증+runner 전달), 계약 테스트, README/conventions의 effort 서술. 실증 (2026-07-16): 이번 round 리뷰가 실제로 gpt-5.6-sol pro effort로 수행됨 — profile에는 enum 부재로 use_for에 임시 기재 중."
    title: "effort 어휘 확장: xhigh 위에 pro(web ChatGPT 경로)와 ultra(codex CLI 경로) 추가 — GPT-5.6부터 'pro'가 model variant가 아니라 effort의 일종으로 취급됨"
    status: done
  - id: fix/release-staging-isolation
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "위임 apply — temp-index 투영 + positive SHIP manifest + commit-tree/CAS, ignored 보존·실패 원상 실 repo 계약. edge 9건은 chore/release-script-hardening. 기록: .waystone/delegations/20260716T095240Z-fix-release-staging-isolation"
    accept: ["release-to-main.sh는 현 워킹트리에 checkout, read-tree -u, rm -rf를 실행하지 않는다 — 임시 index 또는 임시 worktree에서 dev tree를 투영해 release commit을 만들고 main ref만 갱신한다", "배포 대상은 스크립트 내 positive ship manifest(명시적 allowlist)로 정의되며 manifest 밖 경로는 기본 배제된다", "ignored 로컬 파일(.claude/settings.local.json 등)이 성공한 release 후 byte-identical하게 보존된다 (회귀 테스트)", "commit 단계 실패를 주입해도 시작 branch, 워킹트리, 로컬 파일이 원상 유지된다 (회귀 테스트)", "새 manifest 방식의 release tree가 현행 main tree와 동등함이 검증된다 (no-op 동등성)", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    scope: ["release-to-main.sh", "scripts"]
    notes: "리뷰어(chatgpt:gpt-5.6-sol, pro)가 임시 repo에서 재현: clean status에서 release 후 settings.local.json 삭제, status 여전히 clean(무증상 데이터 손실). main-session 코드 검증 일치(checkout main + read-tree -u + rm -rf 경로). SSOT §원칙 Local-first·비파괴 위반. 권고 방향: 임시 GIT_INDEX_FILE/worktree에서 tree 투영(현 누출검증 시뮬레이션과 동일 기법) → release commit → ref 갱신; 현 워킹트리에 checkout/read-tree/rm 금지; 회귀 테스트 2종(ignored 보존 byte-identical, 실패 주입 시 원상태)."
    title: "release-to-main.sh가 현 워킹트리를 release staging으로 사용해 EXCLUDES 경로의 ignored 로컬 파일(.claude/settings.local.json 등)을 rm -rf로 영구 삭제하고, restore가 trap 미등록이라 중간 실패 시 main checkout·부분 변경이 잔류 — 임시 index/worktree 투영으로 재작성"
    status: done
    severity: "major"
    origin: "review-2026-07-16-adopt-dogfooding"
  - id: decision/verifier-hook-isolation-contract
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    ruling: "전 hook hermetic — WAYSTONE_VERIFIER_SESSION 세션에서는 waystone의 모든 hook이 no-op (2026-07-16 사용자 ruling, 리뷰어 권고 채택)"
    title: "WAYSTONE_VERIFIER_SESSION 계약 범위 ruling 필요: 기록 hook 2종만 차단(현행) vs 모든 waystone host hook no-op(hermetic verifier) — 리뷰어는 hermetic이 일관된 경계라고 권고"
    status: done
    origin: "review-2026-07-16-adopt-dogfooding"
  - id: decision/release-ship-manifest
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    ruling: "positive ship manifest — 배포 product 경로를 명시적 allowlist로 정의하고 미등록 경로는 기본 배제 (2026-07-16 사용자 ruling)"
    title: "release 배포 대상 모델 ruling 필요: 현행 denylist(dev tree − EXCLUDES)는 미래 dogfooding artifact 경로를 증명 못함 — positive ship manifest / artifact manifest / contract test 중 택일 또는 현행 유지"
    status: done
    origin: "review-2026-07-16-adopt-dogfooding"
  - id: fix/verifier-hook-hermeticity
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "위임 apply(76bd515) — verifier_guard.sh 전 hook 공용 가드, record-local uv cache, broker 전후 정리(RPC 한계 기록→chore/verifier-guard-residuals-2), RUN 경계 문서화. 적대 리뷰 blocker 0. 기록: .waystone/delegations/20260716T095302Z-fix-verifier-hook-hermeticity"
    accept: ["WAYSTONE_VERIFIER_SESSION=1인 세션에서 hooks manifest의 모든 waystone hook entrypoint가 no-op한다 — 출력, deny, 워크트리·상태 변경 없음 (ruling: 전 hook hermetic)", "verifier 실행의 UV_CACHE_DIR는 리뷰 worktree 밖(record-local 또는 임시 디렉터리)에 위치한다", "verifier hook-matrix 테스트: manifest의 전 hook을 실 subprocess로 실행해 tasks.yaml Read가 deny 없이 통과하고, enable 마커가 있는 Stop 이벤트도 worktree를 바꾸지 않으며, 검증 전후 worktree 상태가 동일함을 확인한다", "verifier가 실제로 worktree 파일을 생성·수정하는 genuine 오염은 여전히 fail-loud로 거부된다 (기존 테스트 유지)", "companion broker로의 guard env 전파와 수명 범위가 명시적으로 처리되거나 한계가 기록된다 (무기록 드롭 금지)", "RUN 단계 구현자 세션의 .waystone 시딩 경계가 처리되거나 명시적으로 기록된다 (무기록 드롭 금지)", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    scope: ["scripts", "hooks"]
    notes: "리뷰어(chatgpt:gpt-5.6-sol, pro)가 tasks_read_nudge 경로를 실 hook+uv로 재현(REAL 확정) — 직전 라운드 적대 리뷰에서 main-session이 '미재현 edge'로 하향했던 판정의 정정. chore/verifier-session-guard-hardening(드롭됨)의 내용 전부 흡수: companion broker의 guard env 수명 범위, RUN 단계 구현자 세션의 .waystone 시딩(무해하나 무기록) 처리 포함. 방향은 decision/verifier-hook-isolation-contract ruling에 종속. 기존 genuine-mutation fail-loud 테스트는 유지."
    title: "verifier 세션에서 발화하는 모든 waystone hook 표면을 격리: 공통 guard를 전 hook entrypoint에 적용(tasks_read_nudge·boundary_check·tasks_guard 포함), UV_CACHE_DIR를 리뷰 worktree 밖으로 이동, manifest의 전 hook을 실 subprocess로 도는 verifier hook-matrix 테스트 추가 — nudge의 deny가 verifier의 Read를 왜곡하는 문제 포함"
    status: done
    severity: "major"
    deps: ["decision/verifier-hook-isolation-contract"]
    origin: "review-2026-07-16-adopt-dogfooding"
  - id: feat/task-status-parked
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "위임 apply — 6번째 상태 parked (선정·주입 제외, 구별 렌더링, archive 비대상, 의존 안전). 인접 갭은 fix/execution-surface-dep-gating. newton은 릴리스 후 task set <id> status parked로 전환 가능. 기록: .waystone/delegations/20260716T104742Z-feat-task-status-parked"
    accept: ["task 상태 어휘에 parked가 추가되어 registry 검증과 waystone task add/set/list가 일관되게 수용한다", "parked task는 next-actionable 선정과 세션 시작 주입에서 제외되고, ROADMAP과 status 표면에서 pending·blocked와 구별되어 표기된다", "parked는 done/dropped가 아니므로 자동 archive 대상이 아니며, 다른 task가 의존해도 안전하고, 착수 후보로 취급되지 않는다", "conventions와 README의 상태 서술이 갱신되어 docs gate를 통과한다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    scope: ["scripts", "hooks", "references", "README.md"]
    notes: "출처: bw2의 newton-omniphysics에서 액체 시뮬레이션 작업을 전략적으로 미루며 상태 부재를 발견 — 임시로 '재개 decision 전 차단' 의존성으로 우회(유효). 외부 리뷰어가 전용 상태 추가 권고, 사용자가 본 repo로 이관. 설계 컷: 상태값 하나 + notes 관행 (단순성 원칙 — 만료/타이머/전용 필드 없음). 소비 지점: registry 검증, task add/set/list 필터, next-actionable 선정(session_context), ROADMAP 렌더러, status 대시보드, archive 선정 로직, conventions/README. 기존 프로젝트 마이그레이션 불필요(워크어라운드 유효, 점진 전환)."
    title: "task 상태 어휘에 parked(의도적 보류) 추가 — 종결도 착수 후보도 아닌 상태: next-actionable·세션 주입 제외, ROADMAP/status에서 blocked와 구별 표기, 자동 archive 비대상. 보류 사유는 notes 관행(전용 필드 없음)"
    status: done
    origin: "newton-omniphysics dogfooding 보고 (bw2, 2026-07-16)"
  - id: fix/runner-env-failure-detection
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "위임 apply — conjunctive 빈-성공 분류(fail-loud), 프리플라이트 프로브, 진단 힌트, 무변경 보호. 잔여는 fix/preflight-probe-isolation. 기록: .waystone/delegations/20260716T142342Z-fix-runner-env-failure-detection"
    accept: ["runner rc가 0이어도 patch가 비고 delegate report가 없으며 runner.stderr에 도구 쓰기 실패 패턴이 있으면, 성공적 needs-review가 아니라 러너 환경 실패로 fail-loud 분류되고 사유가 record에 남는다", "정상 report가 동반된 진짜 무변경 결과는 환경 실패로 오분류되지 않는다", "delegate run이 러너 기동 전 동일 샌드박스 조건의 쓰기 프로브를 수행하고, 실패 시 러너를 띄우지 않고 프로브 stderr 원문과 함께 중단한다", "show --failure가 bwrap/userns/landlock 계열 stderr 패턴을 인식해 진단 힌트를 출력한다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    scope: ["scripts"]
    notes: "출처: spark1(Ubuntu 24.04)의 susradar 프로젝트 — kernel.apparmor_restrict_unprivileged_userns=1 + 비어있는 /etc/apparmor.d/bwrap 프로필로 codex fs sandbox helper(bwrap)가 'loopback: Failed RTM_NEWADDR: Operation not permitted'로 전 쓰기 거부, codex CLI는 rc 0 종료 → waystone이 empty:true/report부재/needs-review 기록(75초 낭비+오분류). 머신 교정 자체(AppArmor 프로필)는 waystone 밖(spark1은 sudo로 해결) — waystone 몫은 실패 분류. spark1 세션 제안 4건 채택: ①rc0+빈patch+report부재+stderr 도구쓰기실패 패턴 조합 → 러너 환경 실패로 자동 분류·fail-loud(사유 record 기록), ②run 전 동일 샌드박스 조건 1회 쓰기 프로브 → 실패 시 러너 미기동 + 'runner sandbox unusable: <stderr 원문>' 중단, ③show --failure가 bwrap/userns/landlock stderr 패턴에 진단 힌트(예: AppArmor userns 제한 의심, /etc/apparmor.d/bwrap 확인), ④진짜 무변경 결과(정상 report 동반)는 오분류 금지. delegate.py 접점 — wave 2 lane E(effort)와 충돌하므로 wave 3에 배치."
    title: "러너 환경 실패의 빈-성공 오분류 수정: codex가 sandbox 쓰기 전면 거부(AppArmor userns/bwrap 등)로 아무것도 못 쓰고 rc 0 종료하면 waystone이 '빈 성공' 계약(needs-review)을 생성 — 환경 실패로 fail-loud 분류 + 프리플라이트 쓰기 프로브 + show --failure 진단 힌트"
    status: done
    severity: "major"
    origin: "spark1/susradar dogfooding 보고 (2026-07-16)"
  - id: chore/verifier-guard-residuals-2
    accept: ["broker pre-cleanup 실패 시 verify가 중단된다 — hermetic 절대 계약(기존 ruling)의 일관 적용; daemon 잔존 상태로 verification을 계속하지 않는다", "companion broker shutdown이 RPC 응답이 아니라 실제 종료·endpoint 소멸을 확인한다", "hook-matrix가 정상 모드(가드 부재)의 실 subprocess 동작도 회귀로 고정한다 (nudge, session_context, resume 포함)", "전체 테스트 스위트와 ruff F401,F841 검사가 통과한다"]
    notes: "출처: fix/verifier-hook-hermeticity 패치의 raw codex 적대 리뷰(blocker 0). 구현 보고 risks에 broker RPC 한계가 이미 기록되어 기준 5('처리 또는 한계 기록')는 충족 — 이 task는 실제 종료 대기(완료 보장) + 정상 모드 회귀 커버리지 확장의 경화분."
    scope: ["scripts"]
    title: "hermeticity 적대 리뷰 잔여 2건: ①companion broker shutdown이 RPC 응답만 확인하고 실제 종료·endpoint 소멸을 대기하지 않음(stale/guarded broker 누출 race — 테스트는 호출 순서만 mock) ②hook-matrix가 가드 모드만 검증, 정상 모드 실 subprocess 커버리지는 nudge/session_context/resume 미포함 + HOME 상속"
    status: "dropped"
    severity: "minor"
    origin: "review-raw-adversarial-20260716"
  - id: fix/publication-gate-bypasses
    accept: ["publication 게이트는 working tree가 아니라 HEAD tree를 증거 소스로 사용한다 — request와 binding을 git ls-tree의 정확한 lexical path로 찾아 HEAD blob에서 직접 읽어 파싱하고, symlink mode는 거부하며, 사이드카 후보 열거도 filesystem glob이 아니라 HEAD tree에서 한다", "skip-worktree나 assume-unchanged로 위장한 tracked 변경이 게이트를 통과하지 못한다", "round id 재사용 시 binding이 현재 closeout에 결속됨이 검증된다 (조상 검사만으로 불충분)", "improve의 파일명과 내장 round_id 불일치 사이드카가 무경고 오귀속되지 않는다 (PR freeze 동일)", "들여쓴 중복 Reviewing 줄이 모호성으로 탐지된다", "삭제된 upstream의 stale tracking ref로 게이트가 통과하지 않는다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "publication 게이트의 config(review.mode, reviews_dir)는 working tree가 아니라 검증 대상 HEAD의 .waystone.yml에서 읽는다 — 위장된 config 변경이 게이트 대상을 재지정할 수 없다", "closeout 결속이 merge 부모 트릭에 안전하다 — 단일 부모 publication 커밋 요구 또는 first-parent 체인 검증으로, 낡은 closeout을 first-parent에 둔 merge가 통과하지 못한다 (real-repo 테스트)", "publication과 improve가 동일한 사이드카 파일명 규칙(binding.json 또는 binding-N.json만)을 단일 소스로 공유한다", "upstream 존재·포함 확인이 축약 DWIM 이름이 아니라 완전한 refs/remotes 참조로 수행되어 tag 섀도잉이나 비활성 refspec 잔존 ref에 속지 않는다", "HEAD 열거가 게이트에 필요한 직계 자식으로 한정되고 무관 경로의 비UTF-8 이름이나 대형 첨부 트리가 게이트를 실패시키지 않는다", "발행 커밋의 변경 경로가 해당 라운드 packet 파일(request/binding sidecar)로 제한되며, 그 외 경로 변경이 포함되면 게이트가 거부한다", "게이트의 모든 git 객체 접근이 시작 시 핀한 불변 SHA 기준으로 일관 수행되고(HEAD 재해석 금지), refs/replace/graft 치환이 무효화된 상태에서 검증된다", "negative refspec으로 fetch에서 제외된 stale remote-tracking ref는 containment 근거로 인정되지 않는다", "sidecar 최신 선택이 timestamp 문자열이 아닌 불변 sequence 우선으로 정렬되어 linear extend/re-close가 통과한다 (positive 재-close 회귀 테스트 포함)", "reviews-tree 열거와 binding-N suffix 파싱이 개수·크기 bounded이며 oversized suffix가 crash가 아닌 명시적 거부로 처리된다", "reviews_dir 경로 검증이 runtime config loader와 단일 공유 규칙이고, stale-closeout-첫부모 거부를 실제 재현하는 fixture가 존재한다"]
    notes: "사용자 재정(2026-07-17, audit 부위 1): 조상-추론 구조를 폐기하고 직접-결속 새 항아리로 재설계 — fix/publication-gate-direct-binding으로 재등록, 구현은 Fable fork. 기존 17기준 중 로컬 공격자 4건(refs/replace·negative-refspec·merge-parent·bounded 열거) 폐기, 사이드카 정렬은 9fbf838로 기해소"
    scope: ["scripts"]
    title: "publication 게이트 edge 우회 5건: reviews 디렉터리 심링크 추적으로 존재검사 오통과, round id 재사용 시 낡은 request/binding이 조상 검사만으로 통과(현재 closeout과 미결속), improve의 파일명↔내장 round_id 불일치 사이드카 무경고 오귀속(PR freeze 동일), 들여쓴 중복 Reviewing 줄 모호성 미탐지, 삭제된 upstream의 stale tracking ref 통과"
    status: "dropped"
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: fix/companion-verifier-effort-forwarding
    accept: ["companion verifier 경로에서 effort가 명시적으로 전달되거나(raw argv 계약 테스트), 전달 불가면 effort is not None을 fail-loud로 거부한다 — 침묵 폐기 금지", "verify artifact에 requested_effort와 effective_effort가 기록된다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    notes: "출처: feat/effort-pro-ultra 패치의 raw codex 적대 리뷰 blocker. 패치 이전에는 verifier effort가 전 transport에서 폐기됐고, 패치가 codex-cli 전달 + pro/ultra 거부를 추가했으나 companion 분기 argv(delegate.py:2074-2075)는 여전히 legacy effort를 침묵 폐기(무기록 대체 금지 위반). 현재 verifier unbind 상태라 즉시 노출 없음 — verify binding 복원 전 처리 권장. wave 3."
    scope: ["scripts"]
    title: "codex-companion verifier transport이 유효 effort 값(xhigh 등)을 argv에 전달하지 않고 무기록 폐기 — effort를 companion에 전달하거나 미지원이면 fail-loud 거부, companion effort 전달 계약 테스트 추가"
    status: "dropped"
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: fix/execution-surface-dep-gating
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "@ c4a89aa: prepare/claim 동일 경로 의존성 게이팅(미존재/미완료 전수 나열, implicit=pending), parked 제외 유지, lanes verify는 ruling대로 전체 nonterminal 검사(--round 시끄럽게 제거, SKILL 원복). claim-드리프트 계약 추가. RED 기록: 위임분 4계약 + --round 거부(사전 rc3). 620 OK, ruff 클린."
    accept: ["delegate run의 packet 구성(prepare와 claim 동일 경로)이 모든 의존 task의 존재와 status done을 강제하고, 실패 시 미충족 의존 id와 상태를 전부 출력한다", "lane 선정에서 parked가 제외된다", "round skill이 waystone lanes verify . --round <round-id>를 사용한다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "lanes verify --round는 해당 round로 stamp된 lane뿐 아니라 아직 stamp되지 않은 비종결(parked 제외) lane도 포함한다 — 스탬핑 전 검증 순서에서 현재 라운드 lane이 건너뛰어지지 않는다 (미스탬프 lane 포함 실 테스트)", "의존 상태 진단이 registry의 유효 상태를 보고한다 — status 필드가 없는 의존은 unknown이 아니라 pending으로 표기된다"]
    notes: "출처: feat/task-status-parked 적대 리뷰 major 2건(+minor: 계약 테스트가 next_actionable만 검사, delegate run/lanes CLI subprocess 미실행). 판정: parked 회귀가 아니라 기존 실행 표면 갭에 parked가 합류한 것 — next_actionable/archive/의존충족/guard/improve는 parked를 올바르게 처리(리뷰 확인). stale start-here 포인터 지적은 비권위 문서 설계로 기각."
    scope: ["scripts"]
    title: "실행 표면의 의존성 게이팅: delegate run이 대상 task 자신의 상태만 검사하고 의존 task 상태(parked/blocked/pending)를 요구하지 않아 미충족 의존의 child를 실행 가능(parked 이전부터의 갭); lanes verify가 parked lane을 in-flight로 취급해 무관한 라운드 검증을 실패시킴"
    status: done
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: chore/release-script-hardening
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "2차 위임 기각(blocker 3) 후 사용자 재정으로 main session 직접 수정 @ e66bbab: env -i allowlist smoke(UV_ENV_FILE/UV_WORKING_DIRECTORY 차단, uv cache·python dir 의도적 통과), TMPDIR 가드 common dir+전체 worktree 확장, manifest 열거 실패 fail-loud. RED-first 기록(수정 전 failures=5), ReleaseToMainTests 18 GREEN, 전체 614 OK, ruff 클린. real-uv 스모크 e2e 계약은 사전-GREEN 가드로 정직 기록."
    accept: ["no-op 판정 경로에 CAS가 적용되어 동시 main 갱신 race에서 거짓 성공을 보고하지 않는다", "릴리스 게이트 실패 주입 테스트가 존재한다 — 테스트 실패 시 main이 전진하지 않음을 real-repo로 고정", "projected tree를 임시 worktree로 materialize해 shipped 상태의 완전성(manifest 미래 누락)을 검사하는 게이트 또는 unknown-toplevel 경고가 추가된다", "commit-tree의 hook 생략과 onbranch:main 서명 의미 변화가 스크립트 주석 또는 문서에 기록되고, TMPDIR가 저장소 내부를 가리키면 거부되며, ref 갱신 후 cleanup 실패가 성공 결과를 실패로 바꾸지 않는다(경고로 강등)", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "projected smoke는 shipped front door(bin/waystone)를 실행해 launcher 부재나 비실행 상태가 게이트를 실패시킨다", "smoke 환경에서 PYTHONPATH 등 dev tree 오염 경로가 격리된다", "unknown-path 경고가 top-level 축약이 아니라 manifest 파일 수준 경로와 대조되어 scripts/ 신규 파일 누락을 탐지하고, lazy import와 중첩 데이터의 잔여 한계는 스크립트 주석에 정직 기록된다", "checked-out-main 검사가 update-ref 직전에 재수행되어 긴 게이트 동안의 TOCTOU 창이 닫힌다", "projected smoke가 UV_ENV_FILE·UV_WORKING_DIRECTORY 등 uv 환경 재주입 경로까지 차단한 격리 환경에서 실행된다 (allowlist 방식, 실 uv 실행 테스트 포함)", "TMPDIR 방어가 현재 worktree root뿐 아니라 git common dir와 모든 연결 worktree 경로를 커버한다", "unknown-path 검출기의 producer(git ls-files) 실패가 전파되어 검출 불능 시 릴리스가 fail-loud로 중단된다"]
    notes: "출처: fix/release-staging-isolation 적대 리뷰(blocker 0, major 5·minor 4) + fix-wave 외부 리뷰. checked-out-main 항목은 fix/release-checked-out-main(major)으로 승격·분리(2026-07-17). 외부 리뷰의 manifest 미래 누락 게이트 제안 채택 방향: projected tree를 별도 worktree로 materialize해 install/smoke 검증하거나 manifest-런타임 의존 완전성 검사 — dev 테스트가 전체 tree에서 돌기 때문에 zero-diff는 시점 증거일 뿐 invariant가 아님. 잔여: no-op CAS, onbranch 서명 기록, 실패 게이트 테스트, commit-tree hook 생략 기록, TMPDIR 방어, index refresh, ref 후 cleanup 실패 순서."
    scope: ["release-to-main.sh", "scripts"]
    title: "release 스크립트 경화 잔여: 체크아웃된 main worktree에서 update-ref 시 불일치, no-op 판정의 CAS 부재(동시 갱신 race), onbranch:main 서명 설정 미적용(의미 변화 기록), 미래 manifest 누락 가드(unknown-toplevel 경고+실 repo 동등성 회귀 테스트), 실패 게이트 테스트 부재, commit-tree의 hook 생략 기록, TMPDIR 저장소 내부 지정 방어, ref 갱신 후 cleanup 실패 시 결과 코드 순서"
    status: done
    severity: "minor"
    origin: "review-raw-adversarial-20260716"
  - id: feat/review-reply-structured-header
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "2차 위임 apply(02bb199) + main-session prefix 상향(12+ hex). 잔여(32KiB 리더 경계, improve 저장값 무재검, raw invalid 병행 노출)는 fix/reply-header-residuals. 기록: .waystone/delegations/20260716T141912Z-feat-review-reply-structured-header"
    accept: ["review packet에 회신 머리에 붙일 기계 파싱용 key: value 블록 템플릿이 포함된다 — 최소 필드 model, effort, review-target(검토 커밋 또는 base-head 범위)이며 템플릿 추가분은 소수 줄로 패킷 토큰 경제를 지킨다", "ingest가 회신 상단의 key: value 블록을 robust하게 파싱한다 — 대소문자, 공백, 필드 순서, 마크다운 코드펜스 유무에 관대하고 웹 UI 복사 텍스트를 전제하며, 알 수 없는 키는 버리지 않고 메타데이터로 보존한다", "누락되거나 손상된 필드는 추측 없이 unknown으로 기록되고 경고를 남긴다 (대체문자나 부분 추정 identity 금지)", "review-target 선언이 해당 round의 binding 대상과 대조되어 불일치 시 시끄러운 경고와 함께 기록된다 — 결속 증거는 ingest 시점의 로컬 연관이 아니라 회신 자체가 담는다", "reviewer_configured는 선언 model이 bind된 route 또는 구성된 리터럴과 문서화된 정규화 규칙 하에 일치할 때만 True이고, 부재나 불일치는 guard(review-skipped-closes-v1)의 configured-feedback으로 인정되지 않으며 경고를 남긴다 (guard 약화 금지)", "강제 재ingest는 구형식 이벤트 행을 포함해 feedback 파일과 canonical overlay 이벤트의 identity를 일관 갱신한다 (모순 기록 금지, 정정 경로 테스트 포함)", "improve의 reviewer 투영이 선언된 model, effort, review-target을 소비하고 unknown은 정직하게 null 처리된다", "파서, round 스킬, review 스킬, 템플릿이 동일한 블록 규칙을 서술하고 템플릿 예시가 실제 파서를 통과하는 round-trip 테스트가 있다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "사이드카가 없는 레거시 round의 ingest는 현재 config나 profile에서 binding을 합성하지 않는다 — 결속 불가를 정직하게 기록하고 경고하며 그 경우 reviewer_configured는 True가 되지 않는다", "저장된 회신 메타데이터의 발견은 파일 전체 스캔이 아니라 header 영역으로 한정되고, 읽기 시 의미 검증(effort 어휘, review-target 형식)을 통과하지 못한 값은 null로 투영된다", "요구 키(model 또는 review-target)가 없는 선두 key: value 산문은 header로 분류되지 않는다 (일반 산문 오분류 방지 부정 테스트 포함)", "effort 어휘 참조가 pro 제거를 반영해 템플릿과 테스트에 pro 잔재가 없다"]
    notes: "전신: feat/reviewer-self-declared-identity(드롭됨) — 위임 2회 기각. 1차 기각: guard 약화(선언 무관 configured=True), force 정정이 overlay 이벤트 미갱신, provenance 부재, invalid UTF-8 추측 기록. 2차 기각(record: 20260716T111216Z): 구형식 이벤트 행(packet:<round>:reviewer:<id>) 정정 불가 — 본 repo의 첫 ingest 행이 해당, ingest-시점 로컬 연관은 재준비 시 재결속됨, 레거시 무사이드카 경로 탐지 사문화, provider-qualified vs 리터럴 정규화 부재, 임의 effort 토큰 수용. 사용자 설계(2026-07-16): 회신 머리 블록 예시 'model: gpt-5.6-pro / effort: high / review-target: a1b2c3-a2b3c4 / foo: bar' — review-target은 검토 커밋(범위) 에코로 결속 증거를 회신 자체에 담음; 패킷 토큰 경제 유지(템플릿 소수 줄); structured output이 아닌 웹 복사 텍스트이므로 엄격 금지·robust 파싱/tagging; 알 수 없는 키도 보존."
    scope: ["templates", "scripts", "skills"]
    title: "리뷰 회신 머리에 기계 파싱용 구조화 key:value 블록(model, effort, review-target 등)을 요구하는 템플릿을 리뷰 패킷에 포함하고, ingest가 이를 robust하게 파싱해 identity·결속 증거로 기록 — 웹 복사 텍스트 전제, guard 약화 금지"
    status: done
    origin: "사용자 설계 (2026-07-16)"
  - id: fix/effort-drop-pro
    session_id: "c555cb27-f338-4324-abc1-d3aeef048585"
    round: 2026-07-16-fix-wave
    result: "위임 apply — enum에서 pro 제거(ultra 유지), 거부 로직·문서·테스트 일관 정리. 근거: 사용자 실측 자기선언 응답. 기록: .waystone/delegations/20260716T135144Z-fix-effort-drop-pro"
    accept: ["effort 어휘에서 pro가 제거되고 ultra는 유지된다 (templates/profile-schema.json과 delegate.py 검증 일치)", "pro 관련 거부 로직, 웹 전용 문서 서술, 테스트가 일관 제거·갱신되고 제거 근거(실측 응답)가 기록에 남는다", "README와 conventions의 effort 서술이 갱신되어 docs gate를 통과한다", "전체 테스트 스위트와 ruff F401,F841 검사가 통과한다"]
    scope: ["templates", "scripts", "README.md", "references"]
    title: "effort 어휘에서 pro 제거 (ultra 유지) — 실측: gpt-5.6-sol pro 모델이 자기선언에서 model: gpt-5.6-pro / effort: high로 응답, provider 내부에서 pro는 effort가 아니라 모델명 축. pro 거부 로직·문서·테스트 일관 제거"
    status: done
    severity: "minor"
    origin: "사용자 실측 (2026-07-16)"
  - id: feat/deterministic-review-packet
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "@ bf991a3 (Fable fork 3차, 사용자 재정 채널): script가 정확 참조 전부 렌더링(모델은 서사만·별도 입력), Reviewing≡exposure closeout 검증, 서사 lookalike 정규화(공백·quote·list·ordered·task-list wrapper), round reclose(host-local 재결속, same-HEAD dirty 거부), PR freeze는 파일 재신뢰 대신 재렌더(재렌더=무결성, 체크섬 0) + 저장 narrative freeze시 재검증. 무조건 마감 단계(질문 금지). codex 리뷰 3회전(마지막 focused BLOCKERS:0, reviews_dir switcheroo는 위협모델 밖 기각). dev 685 OK, ruff 클린. steno sha-hallucination 근본 폐색."
    accept: ["round의 리뷰 요청 문서는 script가 template 기반으로 렌더링한다 — 정확 참조 필드(Reviewing과 diff base sha, 프로젝트, 브랜치, 리뷰어, 회신 header 템플릿 블록)는 script가 git과 binding에서 도출해 채우며 모델 손을 거치지 않는다", "모델이 작성하는 부분은 서사 섹션(변경 의도, 우선 읽을 파일, 공격할 claim, 증거 표, 약점, 도메인 렌즈)에 한정되고 별도 입력 파일로 전달되어 script가 병합한다", "렌더링 시 Reviewing 값이 해당 round binding 대상과 일치함을 script가 검증하고 불일치는 fail-loud로 거부된다", "round skill이 리뷰 요청 생성을 라운드 마감의 무조건 단계로 서술하며 생성 여부를 사용자에게 묻는 선택지가 없다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "서사 입력에 참조 표면 lookalike(열0 '- Reviewing:'/'- Reviewer:'/'- Project:'/'- Branch:' 줄 및 회신 header 키 블록 유사 줄)가 있으면 렌더러가 시끄럽게 거부한다 — 렌더된 요청에서 각 참조 표면은 canonical 1회만 존재", "Reviewing 값은 현재 HEAD가 아니라 round exposure의 closeout head에서 도출되고, 현재 HEAD가 exposure head와 다르거나(스테일) 기존 사이드카와 불일치하면 조용한 supersede 없이 재마감 안내와 함께 fail-loud", "PR 모드의 요청 생성 경로가 실행 가능하게 정의된다 — prepare의 PR 모드 지원 또는 skill의 명시적 PR 경로, 템플릿에 미해석 placeholder 잔존 금지", "서사 lookalike 검증이 줄 선두 공백·blockquote(>)·list wrapper를 정규화한 뒤 매칭한다 — 들여쓰기/인용 포장으로 우회 불가 (우회 계약 테스트 포함)", "신설 round reclose와 PR 캐리어 경계가 적대 리뷰로 판정 완료된다 — reclose는 host-local exposure 재결속만 하며 발행 게이트(pushed HEAD 내 request+binding)를 약화시키지 않는다", "worktree에서 ruff F401,F841 위반 0"]
    notes: "실제 사고: steno round 2026-07-16-review-execution-wave의 packet에서 Reviewing sha가 eaf6c3d5cc0e…로 기재됐으나 실제 closeout은 eaf6c3d8c57e… — 짧은 prefix(eaf6c3d)만 일치, 모델이 나머지 자릿수를 hallucinate. 리뷰어가 fail-closed 거부(검토 중단). 근본 원인: round skill이 모델에게 'git rev-parse HEAD로 Reviewing을 채우라'고 지시 → 모델이 손으로 옮겨 적는 구조. 해결: script 렌더링 + Reviewing≡binding 검증(fail-loud, C2 착지분의 bind 기구 활용) + 서사만 모델 입력. 또한 기존 사례(라운드 마감 선언 후 packet 미생성·'생성할까요?' 질문)도 skill 문구에서 제거. 추가 ruling (2026-07-17): 리뷰어 대상 지침(회신 헤더 블록 등)은 템플릿에서만 정적 렌더링 — skill 산문·전달 프롬프트에 재서술 금지(중복 산문 드리프트 실증: skill의 6–40이 84ad6a7의 12+ 상향을 놓침). script 렌더링이 착지하면 round skill의 keep-verbatim 한 줄도 제거 대상."
    scope: ["scripts", "templates", "skills"]
    title: "리뷰 packet의 결정론 렌더링 분리: 정확 참조 필드(Reviewing/diff base sha, 프로젝트·브랜치, 리뷰어, 회신 header 블록)는 script가 git/binding에서 채우고, 모델은 서사 섹션만 별도 입력으로 작성 — round skill은 packet 생성을 마감의 무조건 단계로 수행(생성 여부 질문 금지)"
    status: done
    severity: "major"
    origin: "B200-2/steno dogfooding 사고 (2026-07-16)"
  - id: feat/review-pending-ledger
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 패치(무원장 파생·4표면·비차단, RED-first 5) + main 마감(3건: 공유 사이드카 정렬 sequence-우선 — 발행게이트 리뷰가 지적한 동일 결함 근본 수정, 완료판정에서 identity 게이트 제거 — 수신≠커버리지, 손상 사이드카 라운드 격리, RED-first 3). 693 OK, ruff 클린."
    accept: ["pending 리뷰(요청은 있으나 feedback 미ingest)가 reviews 디렉터리에서 새 상태 파일 없이 결정론적으로 파생되는 단일 함수가 있고, waystone review pending이 round id, 경과일, 대상 sha, 리뷰어를 나열한다", "SessionStart 주입에 pending 리뷰 요약 한 줄이 추가된다 (0건이면 생략)", "Stop 경계의 waystone check가 pending 리뷰를 비차단 경고로 알린다 — exit code는 변하지 않는다", "round close가 이전 pending 리뷰 존재 시 목록과 함께 리마인드를 출력하되 마감을 막지 않는다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "feedback 완료 판정은 파일 존재가 아니라 ingest 산출 증거(스크립트 작성 메타데이터 헤더 존재)로 한다 — 손으로 복사된 무헤더 파일은 여전히 pending (계약 테스트)", "재발행(binding-N)된 round는 최신 binding이 기준: feedback은 선언된 review-target이 최신 binding target과 일치할 때만 완료로 계산되고, 구 packet에 대한 feedback이 최신 packet의 pending을 침묵시키지 않는다 (기존 header 재계산 재사용, 신규 저장 상태 금지)", "age 표기는 round id·ingest 기록과 동일한 시간대(호스트 로컬) 기준으로 일관된다"]
    scope: ["scripts", "hooks", "skills"]
    title: "미회신 리뷰의 파생 추적(원장 없는 설계): pending = 요청 존재 && feedback 미ingest를 reviews 디렉터리에서 결정론 파생 — waystone review pending CLI, SessionStart 한 줄 주입, Stop 경계(waystone check) 비차단 경고, round close 시 시끄러운 리마인드(비차단). 병렬 라운드·복수 packet 허용 유지"
    status: done
    origin: "사용자 설계 요청 (2026-07-16)"
  - id: feat/waystone-statusline
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 패치(inline 렌더+consent 설치+embed 안내, 실측 50ms·무lock) + main 마감(금지된 렌더별 validate 제거, 읽기불가 reviews 정직 토큰 — RED가 'reviews 0' 부정직 줄 실증, 설치 명령의 launcher 경계 가드). 위임 RED 부재는 착지 시 직접 생성(구현 부재 5 failures). 700 OK, ruff 클린."
    accept: ["waystone statusline이 현재 프로젝트의 한 줄 상태(tasks done/total, 현재 round, pending 리뷰 수, blocker 수)를 수백 ms 내 출력하고 waystone 프로젝트 밖에서는 빈 출력으로 조용히 끝난다", "설치는 optional이며 consent 기록을 남기고, 기존 statusLine 설정이 있으면 덮어쓰지 않고 waystone statusline을 기존 스크립트에 embed하는 안내를 출력한다", "한 줄 렌더링은 ANSI 색과 글리프를 활용하되 정보는 파생 상태만 사용하며 모델 토큰을 쓰지 않는다", "pending 리뷰 수는 feat/review-pending-ledger의 단일 파생 함수를 재사용한다 (중복 구현 금지)", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "statusline 렌더 경로는 어떤 파일도 변조하지 않는다 — legacy 전용 프로젝트(waystone.yml만 존재)에서 config 개명 migration을 지나지 않고 조용히 빈 출력으로 종료 (실파일 확인 계약 테스트)", "프로젝트 내부의 손상 registry/config에서 rc 1이나 stderr 대신 정직한 한 줄 표시(예: registry unreadable)를 stdout rc 0으로 렌더한다 — 프롬프트 라인은 절대 깨지지 않는다"]
    scope: ["scripts", "skills"]
    title: "waystone statusline 명령 + optional 설치: 파생 상태만으로 fancy 한 줄(tasks done/total, 현재 round, pending 리뷰 수, blocker 수) 출력, init/install에서 consent 기반 설치하되 기존 statusLine 설정은 덮어쓰지 않고 embed 안내"
    status: done
    deps: ["feat/review-pending-ledger"]
    origin: "사용자 설계 요청 (2026-07-16)"
  - id: fix/preflight-probe-isolation
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 패치(sibling 격리·기록 내구성·stale 정리) + main 마무리(실용 최소 ruling): seatbelt/sandbox-exec 어휘·macOS 힌트, probe 내부 결함의 정직한 internal 분류(transport 위장 금지), 분류기=heuristic hint 문서화(양방향 완전성 불추구). RED 기록: 위임 7계약 + main 2계약(사전 failures=2). 653 OK, ruff 클린."
    accept: ["프로브가 본 worktree를 오염시킬 수 없다 — 동일 cache·filesystem 조건의 일회용 sibling worktree에서 실행 후 폐기하거나, 동일 worktree 유지 시 프로브 전후 tracked·untracked·ignored manifest byte 대조로 reserved 외 변경을 fail-loud하고 원상 검증 후에만 본 러너를 기동한다", "stderr 분류 패턴이 양방향 정확화된다 — 무관 permission-denied가 오분류되지 않고 어순 변형 Landlock 실패가 탐지된다", "프로브 실패의 transport(인증·네트워크) 원인이 sandbox 문제로 오진되지 않고 구분 기록된다", "프로브 비용(시간)이 record에 계상된다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "분류 재현 케이스 통과: 'Landlock setup failed: permission denied'=sandbox, 성공 서술('correctly denied … all tests passed')=hint 미발화, bwrap network namespace 실패=transport 아닌 sandbox (순서·lookahead 어휘 보정 + 케이스 고정 테스트)", "프로브 증거 수집 실패(파일 chmod 000, stderr 읽기 오류 등)가 예외로 새지 않고 probe 결과와 duration이 실패 경로에서도 기록된다 (계약 테스트)", "sibling worktree 생성 타임아웃/부분 성공이 stale 등록을 남기지 않는다 — 생성 시도를 기록 후 정리를 책임지고, 잔존 시 다음 run이 명시 진단과 함께 자동 정리 또는 안전 중단한다"]
    notes: "출처: fix/runner-env-failure-detection 적대 리뷰(blocker 0, major 4·minor 3). 수용 근거: 분류 게이트는 conjunctive(정직 무변경+report 보호 확인), 오류 방향이 fail-loud라 silent 성공보다 보수적. 잔여: 프로브 후 상태 검증(reserved 파일만 확인 중), 패턴 anchoring, 오진 구분, hint 토큰 오탐(‘all Landlock tests passed’), 프로브 duration 미계상, 실 bwrap/seatbelt 무테스트."
    scope: ["scripts"]
    title: "프리플라이트 프로브 경화: 프로브(모델 턴)가 남긴 프로브 외 편집이 main run patch에 출처 오염 가능 — 프로브 후 git 상태 대조·정리 필수; stderr 패턴 양방향 정확화(무관 permission-denied 오분류/어순 변형 Landlock 미탐); 프로브 실패의 transport(인증·네트워크) vs sandbox 구분; 프로브 비용 기록·계상"
    status: done
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: fix/reply-header-residuals
    accept: ["32KiB 초과 feedback에서도 구분자가 조기 등장하면 유효 메타를 읽는다 (경계 테스트 포함)", "ingest가 소비한 sidecar 경로, digest, target/base/reviewers tuple이 immutable하게 기록되고, projection의 review_target_matches와 reviewer_configured는 저장 boolean 신뢰가 아니라 그 스냅샷 재검증(또는 immutable ingest event 권위)으로 산출된다 — 검증할 binding이 없으면 null", "정규화에 실패한 invalid 값이 raw reply_metadata로 병행 노출되지 않는다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "ingest event의 binding 스냅샷(target/base/reviewers/digest)이 자체 무결성 체크섬으로 봉인되고, projection이 feedback 실해시와 스냅샷 체크섬을 재계산 검증한다 — 불일치는 감지로 표면화(null+경고), 저장 digest가 장식이 아니다", "스냅샷 변조 계약 테스트: ingest-event 행의 tuple/digest 편집이 projection 사실을 바꾸지 못하고 감지로 표면화된다 (실 subprocess)", "32KiB 경계를 실제로 걸치는(시작<cap<끝) separator 회귀 테스트가 존재한다"]
    notes: "2차 소진 후 사용자 재정(2026-07-17): 체크섬/봉인 설계 자체를 폐기(참고 기록에 과잉) — 쓰기 규율+읽기 재계산 설계의 fix/review-feedback-triage-discipline로 대체"
    scope: ["scripts"]
    title: "구조화 회신 헤더 잔여: 32KiB 초과 feedback에서 구분자 조기 등장 미확인으로 유효 메타 소실(무경고 null), improve가 저장 boolean(review_target_matches/reviewer_configured)을 사이드카 재검 없이 신뢰, 정규화 null 처리된 invalid 값이 raw reply_metadata로 병행 노출"
    status: "dropped"
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: fix/release-checked-out-main
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 apply — worktree preflight 가드, real-repo 계약 2건 + 라이브 재검. 기록: .waystone/delegations/20260716T155505Z-fix-release-checked-out-main"
    accept: ["refs/heads/main이 현재 또는 다른 linked worktree에 checkout되어 있으면 release가 시작 전에 해당 worktree 경로를 출력하며 fail-loud로 중단한다 (git worktree list --porcelain 검사)", "현재 worktree가 main인 경우와 다른 linked worktree가 main인 경우가 각각 real-repo 테스트로 재현·고정된다 (RED-first)", "전체 테스트 스위트와 ruff F401,F841 검사가 통과한다"]
    notes: "리뷰어(gpt-5.6-pro, high)가 재현: main checkout 상태에서 update-ref 후 'M README.md' + release 역방향 cached diff — 이후 평범한 commit이 release를 역전 가능. 비파괴 계약·release provenance 직접 위협이라 chore/release-script-hardening의 minor 항목에서 major 단독 task로 승격(리뷰어 권고 수용). 기존 회귀 테스트 factory가 항상 dev checkout이라 미커버."
    scope: ["release-to-main.sh", "scripts"]
    title: "main이 어느 worktree에든 checkout된 상태에서 release가 성공하면 그 worktree의 HEAD만 이동하고 index/파일은 구 tree로 남아 release 역방향 staged diff가 생김 — 시작 전 git worktree list 검사로 fail-loud"
    status: done
    severity: "major"
    origin: "review-2026-07-16-fix-wave"
  - id: fix/drop-codex-companion
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 apply(5cefec0) + main-session 정합(manifest 문구·schema entry enum·메시지). verify = host 무관 codex exec 단일 경로. 잔여는 fix/codex-exec-verifier-hardening. 기록: .waystone/delegations/20260716T160036Z-fix-drop-codex-companion"
    accept: ["waystone에서 codex-companion 경로가 완전히 제거된다 — delegate.py의 companion transport 분기, broker 탐지·정리(RPC) 로직, 관련 상수·테스트 픽스처, 스킬·README·conventions의 서술 전부 (companion/broker 잔재 grep 0건)", "verifier의 codex 실행이 host와 무관하게 codex exec 단일 경로다 — waystone 소유 adversarial-review 프롬프트 템플릿과 verifier-output-schema 검증으로 verify-N.json을 생성하고, read-only sandbox와 --output-last-message를 사용하며 codex-cc plugin에 대한 어떤 의존도 없다", "verifier binding의 effort가 codex exec에 model_reasoning_effort로 정확히 전달되고(raw argv 계약 테스트) verify artifact에 requested_effort와 effective_effort가 기록된다", "레거시 profile 값(execution: codex-companion|codex-cli, entry: adversarial-review)은 실패 없이 정규화되어 동작하고 deprecation 안내를 출력한다 — 스키마와 런타임이 일치한다", "WAYSTONE_VERIFIER_SESSION guard가 companion 없이 codex exec subprocess env로 직접 전파되고 hermeticity 계약 테스트가 유지된다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다"]
    notes: "사용자 지시 (2026-07-17): companion(codex-cc plugin의 Node broker 런타임) 의존을 설계·구현에서 완전 삭제. 흡수: fix/companion-verifier-effort-forwarding(드롭됨 — effort 침묵 폐기는 companion 소멸로 근본 해소, 단 codex exec 경로의 effort 정확 전달과 requested/effective 기록은 본 task 기준으로 승계), chore/verifier-guard-residuals-2의 broker 항목들(broker 자체 소멸). verify는 기존 verifier-output-schema.json을 재사용해 codex exec --output-last-message 출력을 검증. 레거시 profile(execution: codex-companion|codex-cli, entry)은 정규화+deprecation 안내(0.9 마이그레이션 관례). WAYSTONE_VERIFIER_SESSION 전파는 subprocess env 직접 — hermeticity 계약 테스트 유지."
    scope: ["scripts", "skills", "templates", "README.md", "references"]
    title: "codex-companion 경로 전면 제거 — verifier의 codex 실행을 host 무관 codex exec 단일 경로로 통일 (waystone 소유 adversarial-review 프롬프트 + verifier-output-schema 검증), broker 탐지·정리 로직 삭제, codex-cc plugin 의존 0"
    status: done
    severity: "major"
    origin: "사용자 결정 (2026-07-17)"
  - id: chore/hook-matrix-normal-mode-coverage
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 패치(양모드 실 subprocess 매트릭스+mutation-kill 6/6+명시 env) + main 마무리(스냅샷 약화 복원: _verify_worktree_state 동등성, lstat mode·symlink identity, 정상모드 HEAD/status 불변) @ 커밋. 661 OK, ruff 클린."
    accept: ["plugin hooks.json에 등록된 전 hook 스크립트(boundary_check + verifier_guard 경유 전체, nudge/session_context/resume 포함)가 정상 모드(WAYSTONE_VERIFIER_SESSION 부재)에서 실 subprocess로 기대 동작을 내는 회귀 테스트가 존재한다", "hermetic 모드(WAYSTONE_VERIFIER_SESSION=1)와 정상 모드의 hook별 동작 차이가 같은 매트릭스에서 명시적으로 고정된다", "hook 테스트가 호출 환경의 HOME을 상속하지 않는다 (테스트 자체 HOME 고정)", "전체 테스트 스위트(신규 계약은 RED-first)와 ruff F401,F841 검사가 통과한다", "매 hooks.json entry가 선행 hook 실행 상태에 오염되지 않는 인과 독립 단언을 가진다 — 해당 entry 명령을 no-op으로 치환하면 그 단언이 실패함을 mutation 실행으로 실증하고 보고서에 기록 (SessionEnd 포함)", "정상/hermetic 쓰기 차이가 hook별로 고정된다 — 각 hook 실행 후 worktree와 synthetic HOME 양쪽을 대조하고, session_context 정상 모드의 WAYSTONE_HOME 쓰기는 존재(정상)/부재(hermetic)로 명시 단언", "HOME 격리의 의미를 명확화: hook subprocess 환경은 명시 구성되어 호출자 HOME이 hook 프로세스에 도달하지 않으면 충족 — fixture 준비 명령(git, uv cache dir)의 caller-env 사용은 스위트 관례상 허용"]
    notes: "전신 chore/verifier-guard-residuals-2(드롭됨)에서 생존한 항목 — broker 관련 항목(RPC 완료 대기, pre-cleanup fail-closed)은 fix/drop-codex-companion의 companion 전면 제거로 소멸."
    scope: ["scripts", "hooks"]
    title: "hook-matrix 회귀를 정상 모드(가드 부재)까지 확장: nudge, session_context, resume 포함 전 hook의 실 subprocess 동작을 양 모드에서 고정 (HOME 상속 정리 포함)"
    status: done
    severity: "minor"
    origin: "review-raw-adversarial-20260716"
  - id: fix/codex-exec-verifier-hardening
    notes: "사용자 재정(2026-07-17): legacy 하위호환 2건(구 waystone-verify-1 artifact, on-disk legacy profile e2e)은 audit 판정대로 폐기 — 사용자 1명·in-flight 위임 0인 지금이 폐기 적기. transport 실패경로만 fix/verifier-transport-hardening으로 재등록"
    scope: ["scripts"]
    title: "codex exec 단일 verifier의 잔여 경화: 구 waystone-verify-1 artifact(requested/effective effort 부재)의 하위호환 검증(in-flight 위임이 upgrade를 넘어도 verdict 가능), on-disk 레거시 profile(codex-cli|codex-companion+entry)의 e2e 회귀, transport 실패경로(timeout·비정상 종료 stderr 보존·빈 출력·재시도 시 stale output 정리) 계약 테스트"
    status: "dropped"
    severity: "major"
    origin: "review-raw-adversarial-20260717"
  - id: decision/lanes-verify-round-scope
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "Ruling(사용자 2026-07-17): lanes verify는 종결 상태(done/dropped/parked) 제외 전체 nonterminal lane을 매 마감마다 검사 — 스탬프 무관이라 재개 lane 우회 불가. 무관한 open lane의 깨진 브랜치가 마감을 막는 것은 fail-loud 의도이며 의도적 보류는 parked가 탈출구. --round 인자는 선택 필터에서 제거/보고용 강등. 수정 경로: main session 직접(2차 소진 후 재정)."
    title: "lanes verify의 검사 범위 의미론: round 스탬프는 마감 후 부여되므로 '이번 라운드 lane'을 마감 전에 식별 불가 — 검사 대상을 무엇으로 정의하는가 (전체 nonterminal / round 연관 필드 신설)"
    status: done
    severity: "blocker"
    deps: ["fix/execution-surface-dep-gating"]
    origin: "review-raw-adversarial-20260716"
  - id: fix/review-feedback-triage-discipline
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "@ f898eb3: 위임 2차 패치(마커 섹션+review triage 명령+읽기 재계산+round 불일치 null+중복키 non-부활+32KiB 경계, RED-first 6) + main 마무리(2차 소진 후 재정): anchor를 검색→산술로 교체(ingest가 verbatim-bytes를 헤더에 기록, triage는 위치 도출+바이트 대조) — 인용 마커 masking 클래스 소멸, 리뷰어 반례 계약 고정(RED: 구 anchor rc0 파괴). 661 OK, ruff 클린. 무결성 기계 0."
    accept: ["feedback 파일 구조가 '메타데이터 헤더 + verbatim 회신 + 맨 끝 마커로 구분된 triage 섹션'이 된다 — ingest가 마커 포함 skeleton을 append하고, 신설 waystone review triage 명령이 마커 구간만 교체하며 교체 전후 앞부분이 byte-identical함을 계약 테스트로 고정", "review triage 명령은 마커 부재/손상 시 조용한 재생성 없이 시끄럽게 거부한다", "improve/overlay projection의 reviewer_configured·review_target_matches가 저장 boolean 신뢰 없이 projection 시점에 feedback 헤더 재파싱+binding 대조로 재계산된다 — 파일/헤더/binding 부재나 파싱 실패는 정직한 null(+사유), 체크섬·스냅샷·봉인 기계는 도입하지 않는다", "32KiB cap을 걸치는(시작<cap<끝) separator가 유효 메타로 읽힌다 (경계 회귀 테스트 포함)", "정규화에 실패한 invalid 값이 raw reply_metadata로 병행 노출되지 않는다", "review skill의 triage 절차가 파일 직접 편집 대신 신설 명령 사용으로 갱신된다", "전체 테스트 스위트(신규 계약 테스트는 RED-first)와 ruff F401,F841 검사가 통과한다", "reviewer 회신 본문이 마커 문자열을 인용해도 triage가 영구 거부에 빠지지 않고 verbatim 바이트가 교체로 파괴되지 않는다 — 마커 탐지는 내용 전역 검색이 아닌 구조 앵커(마지막 BEGIN + EOF 종단 END, 또는 ingest 기록 offset 권위)로 하며 인용 충돌 계약 테스트 포함", "같은 표준 키의 유효 선언 뒤 손상(invalid-UTF8) 중복 선언이 오면 그 키는 None이고 어떤 값도 raw reply_metadata로 부활하지 않는다", "feedback 헤더에 기록된 round가 projection의 기대 round와 다르면(파일 복사/개명) configured가 아니라 null+round-mismatch 사유로 투영된다"]
    scope: ["scripts", "skills"]
    title: "feedback 파일 쓰기 규율로 회신 검증 단순화: triage를 마커 구분 섹션으로 분리하고 script가 그 구간만 교체(앞쪽 verbatim 불가침), projection은 저장 boolean 대신 읽기 시점 재파싱+binding 대조로 재계산 — 체크섬/봉인 불채택(ruling 2026-07-17)"
    status: done
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: fix/task-cli-arg-validation
    notes: "2차 소진 후 사용자 재정(2026-07-17): 중앙 preflight(병렬 문법) 접근 폐기 — chokepoint 설계로 fix/cli-uninitialized-root-gate 재등록, 구현은 Fable subagent"
    accept: ["task 서브커맨드(add/set/drop/show/list/archive)가 미지원 옵션을 즉시 오류로 거부하며 옵션 문자열을 root 경로로 오인하지 않는다 (drop --reason 사고 재현 계약 테스트, RED-first)", "migration/profile seeding은 초기화된 프로젝트 root(.waystone.yml 존재)에서만 동작하고 임의 경로에 디렉터리를 생성하지 않는다", "task drop이 --reason 옵션을 지원해 사유가 해당 task notes에 기록된 뒤 drop된다", "전체 테스트 스위트(신규 계약 RED-first)와 ruff F401,F841 검사가 통과한다", "front door(waystone task ...)에서 미지원 옵션 거부가 registry lock 생성과 home migration보다 먼저 수행된다 — 인자 검증은 순수 파싱이므로 lock 불요", "명시 root 인자를 받는 모든 명령군(tasks/delegate/overlay/review/round 등)이 미초기화 root(.waystone.yml 부재)에 .waystone/lock mkdir 없이 실행 전 명시 오류를 낸다 — root를 새로 초기화하는 명령(init)만 명시 예외", "'--'로 시작하는 정당한 옵션 값이 --flag=value 형태(또는 동등 이스케이프)로 전달 가능하고 값 위치 토큰을 옵션으로 오분류하지 않는다 (--title '-- follow-up' 회귀 테스트)", "RED-first 증거가 케이스별 개별 실행으로 기록된다"]
    scope: ["scripts"]
    title: "task CLI 인자 검증 부재: task drop <id> --reason <문장>이 미지원 플래그를 조용히 root 경로로 오인 — 존재하지 않는 root에 migration이 profile을 auto-seed(디렉터리 생성 부작용)한 뒤 crash. 미지원 옵션은 즉시 거부하고, migration seeding은 초기화된 프로젝트 root에서만 동작해야 함"
    status: "dropped"
    severity: "major"
    origin: "dogfooding-20260717"
  - id: decision/deterministic-workflow-carrier-semantics
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    title: "ADR: deterministic-workflow의 carrier·귀속 의미론 확정 (execution=절차 정의, carrier 축, Codex fail-loud, backend·effort 규칙)"
    status: done
  - id: feat/delegate-fanout-cli-contract
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    scope: ["scripts/delegate.py"]
    title: "delegate CLI orchestration-safe 경화: plan --json(manifest·dep gate·corrupt fail-closed), run --expect-packet-sha/--expect-profile/--carrier/--json-events, status --json"
    status: done
    deps: ["decision/deterministic-workflow-carrier-semantics"]
  - id: feat/deterministic-workflow-carrier-contract
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    scope: ["skills/delegate", "docs/CONVENTIONS.md", "references"]
    title: "delegate 스킬·CONVENTIONS·main-contract에 deterministic-workflow 캐리어 계약(binding 우선·carrier report 비권위·Codex fail-loud) 명문화"
    status: done
    deps: ["decision/deterministic-workflow-carrier-semantics"]
  - id: feat/delegate-fanout-workflow-template
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    scope: ["templates/hosts"]
    title: "CC 캐리어 템플릿 templates/hosts/claude-code/delegate-fanout.workflow.js + validateOnly compile gate + 시나리오 matrix bake"
    status: done
    deps: ["feat/delegate-fanout-cli-contract"]
  - id: chore/remove-codex-companion-transport
    title: "codex-companion transport 완전 제거 — codex backend verifier를 호스트 불문 codex exec로 통일, ultra effort 관통, WAYSTONE_HOST 분기·legacy transport 정리"
    status: "dropped"
  - id: chore/overengineering-prune-batch1
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 apply(리뷰 blocker 0, 8 hunk 전수 순삭제 확인) + main이 겸유 커버리지(conflicting-pr-freeze-sidecars) 복원. 661 OK, ruff 클린."
    accept: ["discard가 discarding 상태 재개 시 원래 reason의 정확 재현을 요구하지 않는다 — 새 reason 또는 자동 승계로 discarded 전이하며 관련 테스트는 새 계약으로 교체된다", "claim 경로의 packet 검증에서 중복 다이제스트 재도출이 제거되어 단일 검증만 남는다 — run --expect-packet-sha의 fan-out 스테일 차단은 유지", "improve._review_sha_binding wrapper와 전용 테스트가 삭제된다 (프로덕션 호출자 0 재확인 포함)", "_finding_evidence의 '..' 경로 거부 분기가 제거된다 (해당 문자열은 어디서도 dereference되지 않음을 근거로)", "전체 테스트 스위트와 ruff F401,F841 통과 — 기존 계약 약화는 삭제 대상 자체의 전용 테스트에 한정"]
    scope: ["scripts"]
    title: "오버엔지니어링 전수조사 A-2 순삭제 묶음: discard 재개 시 reason 정확 재현 강제 제거, packet 3중 검사에서 claim측 다이제스트 재도출 축약, improve._review_sha_binding dead wrapper 삭제, _finding_evidence의 '..' 경로 거부 제거 (근거: dev_docs/overengineering-audit-2026-07-17.md)"
    status: done
    severity: "minor"
    origin: "overengineering-audit-20260717"
  - id: fix/reply-header-parser-simplification
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 패치(헤더 블록 한정 단일 decode 규칙, 중복 키 None+경고 유지, RED-first 4) + main 마무리(U+FFFD 잔존 분기 삭제, 스캔 슬라이스 선행). 리뷰 blocker 중 'byte-damaged 중복의 필드별 종착지 요구'는 삭제 대상 기계 재요구로 기각(결정 표면 동일). 662 OK, ruff 클린."
    accept: ["parse_review_reply_header의 줄별 UnicodeDecodeError 복구·중복 키 특례·surrogate 거부 분기가 제거되고 '헤더 블록 통 UTF-8 decode 실패 = 헤더 없음(detected=false)' 단일 규칙으로 대체된다 — 모든 기존 종착지(필드 None→not-configured)는 동일 유지", "read_feedback_reply_metadata의 자기 산출물 필드별 적대 재검증이 제거된다 — 파일/구조 부재의 정직한 null 경로는 유지", "유효 헤더·헤더 없음·손상 헤더·32KiB 경계의 기존 계약은 전부 GREEN 유지되고, 제거되는 특례 전용 테스트만 새 규칙 계약으로 교체된다", "전체 테스트 스위트(교체 계약은 RED-first)와 ruff F401,F841 검사가 통과한다", "통 decode 규칙의 대상은 헤더 블록(선두부터 헤더 종단까지)이며 본문 바이트는 판정에 관여하지 않는다 — 고정 슬라이스 경계에서 유효 멀티바이트가 잘려도 유효 헤더는 detected=true (비ASCII 장문 회신 계약 테스트)", "같은 표준 키의 중복 선언(유효→유효 포함)은 해당 키 None+경고로 유지된다 — last-wins 금지"]
    title: "회신 헤더 파서 corner 기계 제거: byte cap 내 줄별 UnicodeDecodeError 복구·중복 키 특례·surrogate 거부를 '통 decode 실패=헤더 없음' 한 줄로 대체, read_feedback_reply_metadata의 자기 산출물 적대 재검증 제거 (근거: audit 부위 2)"
    status: done
    severity: "minor"
    deps: ["fix/review-feedback-triage-discipline"]
    origin: "overengineering-audit-20260717"
  - id: fix/cli-uninitialized-root-gate
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "@ 6a9e0a1 (Fable fork 구현, 사용자 재정 채널): 게이트 프리미티브 2종(hold_project_lock/ensure_project_state_dir)에 단일 chokepoint — 19개 호출처 교체, 중앙 문법 0, resume까지 편입(리뷰 blocker 반영). 서브커맨드별 allowlist·--flag=value·값누락 시끄러운 오류·drop --reason. 무회귀 핀(subdir walker·stale unregister·init 순서). codex 리뷰 1회전+잔여 3건 수정. 673 OK, ruff 클린. config 유효성 게이트는 과잉으로 불채택."
    accept: ["프로젝트 lock 획득/.waystone 생성이 일어나는 단일 지점이 미초기화 root(.waystone.yml 부재)에서 디렉터리 생성 없이 명시 오류로 거부한다 — root를 새로 초기화하는 명령(init)만 명시 예외, waystone.py에 중앙 문법 preflight를 추가하지 않는다", "tasks.py 파서가 미지원 옵션을 즉시 거부하고(사고 재현 계약), '--'로 시작하는 값은 --flag=value로 전달 가능하며 bare '--flag -- value'는 silent 오분류 없이 명시 동작한다", "task drop --reason이 지원되어 사유가 notes에 기록된 뒤 drop된다", "정당 흐름 무회귀: 상향 탐색 명령의 --root subdir, stale 경로의 project unregister, init e2e, 기존 전 서브커맨드 플래그가 그대로 동작한다", "전체 테스트 스위트(케이스별 RED-first 개별 기록)와 ruff F401,F841 검사가 통과한다"]
    scope: ["scripts"]
    title: "CLI 미초기화 root 쓰기 차단을 단일 chokepoint로: 문법은 각 모듈 소유 유지(중앙 preflight 금지), mkdir가 일어나는 프로젝트 lock 획득 지점에서만 초기화(.waystone.yml) 요구 — task drop --reason 사고의 구조적 재설계 (Fable subagent 구현, 사용자 재정 2026-07-17)"
    status: done
    severity: "major"
    origin: "dogfooding-20260717"
  - id: fix/probe-once-config-gate
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 패치(1회 프로브+서지컬 기록+skip, CRLF/alias/0444 편입) + main 마감 4건(merge-key 거부, null/false→true 치환 — 영구 재프로브 차단, mode 보존, generic failed-env 힌트). RED x4(사전 6 failures). 715 OK, ruff 클린. 명시 null 거부는 기각 유지"
    accept: [".waystone.yml delegation 아래 codex_runner_verified key — 미설정이면 기존 프로브를 1회 실행하고, 성공 시 true를 주석 보존 서지컬 편집으로 기록하며, true로 설정된 뒤에는 프로브를 완전히 생략한다 (생략 계약 테스트 포함)", "프로브 실패 시 key를 기록하지 않고 기존 거부 동작을 유지한다(다음 실행이 재프로브); 사후 검출기의 failed-env 진단 힌트에 key 재검토 안내를 추가한다", "config normalize/스키마에 key가 반영되고, 기존 프로브 계약 테스트는 key-부재 경로로 전부 유지된다", "전체 테스트 스위트(신규 계약 RED-first)와 ruff F401,F841 검사가 통과한다", "서지컬 편집이 개행 스타일(CRLF 포함)을 바이트 그대로 보존하고, delegation 블록이 YAML alias/anchor를 경유하면 편집을 거부하며 수동 추가를 안내한다 (실파일 계약 테스트 — in-memory 문자열 아님)", "config가 쓰기 불가(읽기 전용 포함)면 조용한 교체 없이 기존 쓰기 실패와 동일하게 시끄럽게 실패한다"]
    scope: ["scripts", "templates", "skills"]
    title: "프리플라이트 프로브를 1회성 검증으로: .waystone.yml delegation.codex_runner_verified key가 미설정일 때만 프로브 실행, 성공 시 true를 주석 보존 편집으로 기록, 이후 참조만 하고 프로브 생략 (사용자 설계 2026-07-17)"
    status: done
    severity: "minor"
    origin: "overengineering-audit-20260717"
  - id: chore/migration-sunset
    notes: "실행 보류(사실상 parked — 릴리스판 하네스가 parked 상태를 아직 몰라 pending 유지): 모든 머신 0.11+ 이관 확인 후 착수. 2026-07-17 사용자 승인"
    title: "legacy migration 서브시스템 sunset: 모든 머신이 0.11+ 이관 완료 확인 후 pre-0.9 마커·재개·자동 repair 상태기계 통째 삭제 (audit 부위 5, 사용자 승인 — 실행은 이관 확인 후)"
    status: "pending"
    severity: "minor"
    origin: "overengineering-audit-20260717"
  - id: fix/verifier-transport-hardening
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "위임 2차 apply(리뷰 실계약 전부 생존): timeout 예산·signal 명명·빈/공백/미생성 출력 명시 실패·절단실패 loud. blocker(verify-N 무효화)는 범위 밖 과잉으로 기각, 보고 계상 오류(+3/718→실제 +4/719) 기록. 719 OK, ruff 클린"
    accept: ["verifier codex exec 호출의 타임아웃과 비정상 종료에서 stderr가 record에 보존되고 원인이 진단에 나타난다", "빈 출력(0바이트 last-message)은 성공으로 위장되지 않고 명시 실패로 분류된다", "verify 재시도 시 이전 시도의 stale output 파일이 새 결과로 오인되지 않는다 (정리 또는 시도별 격리)", "전체 테스트 스위트(신규 계약 RED-first)와 ruff F401,F841 검사가 통과한다", "재시도 시 이전 stderr/jsonl 절단·정리 실패는 조용히 삼켜지지 않고 시끄럽게 실패한다 — stale 내용이 새 시도의 진단으로 표시될 수 없다 (절단 실패 주입 계약 테스트)", "음수 rc는 시그널로 해석되어 진단에 시그널 번호와 이름(예: signal 9 SIGKILL)이 표기된다", "whitespace-only 출력과 출력 파일 미생성도 0바이트와 같은 명시 실패 클래스로 계약 테스트에 고정된다"]
    scope: ["scripts"]
    title: "codex exec verifier의 transport 실패경로 경화: 타임아웃·비정상 종료 시 stderr 보존, 빈 출력 정직 처리, 재시도 시 stale output 정리 (verifier-hardening에서 legacy 하위호환 폐기 후 승계)"
    status: done
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: fix/publication-gate-direct-binding
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    result: "@ 커밋 (Fable fork + codex 리뷰 2회전 + main 마감): 직접-결속 2검사 판정, 조상-추론 클래스 전면 삭제(remote --round head_pushed 포함), committed-unpushed RED 고정, 핀 SHA 출력 재결속, 재발행 중간상태 거부, source 계약 공백-정규화. 706 OK, ruff 클린. 기준 17→6 순감의 새 항아리 완성"
    accept: ["remote verify --round의 발행 판정은 (1) binding이 가리키는 closeout SHA가 원격 추적 ref에 containment (2) 그 커밋 트리에서 request와 binding 사이드카의 바이트가 로컬과 일치 — 두 가지만으로 결정되고 merge topology·first-parent 체인·HEAD 재해석 검사는 존재하지 않는다", "검증 시작 시 대상 SHA를 1회 해석해 이후 모든 git 접근이 그 불변 SHA를 쓴다 (심볼릭 재해석 금지 — 값싼 정확성)", "round id 재사용/재마감 시 최신 binding(sequence 우선, 9fbf838 정렬)이 기준이고 낡은 request/binding은 발행 근거가 되지 않는다", "reviews 디렉터리 심링크로 존재검사를 속일 수 없고(실파일 요구), 들여쓴/중복 Reviewing 줄 모호성은 기존 strict 파서 계약 유지", "정상 흐름 무회귀: linear extend·재마감(reclose 포함)·legacy 라운드 정직 강등이 기존 계약대로 통과하고, 제거되는 조상-추론 전용 테스트만 새 직접-결속 계약으로 교체된다", "전체 테스트 스위트(신규 계약 RED-first)와 ruff F401,F841 검사가 통과한다"]
    scope: ["scripts"]
    title: "발행 게이트 직접-결속 재설계: 게이트는 binding 사이드카가 가리키는 closeout SHA의 원격 containment와 그 커밋 트리 안 packet 파일(request/binding) 바이트 대조만 검증 — 조상 추론(merge topology·first-parent·HEAD 재해석) 클래스 제거 (audit 새 항아리, 사용자 승인 2026-07-17, Fable fork 구현)"
    status: done
    severity: "major"
    origin: "review-raw-adversarial-20260716"
  - id: docs/readme-staleness-sweep
    session_id: "4ba467a5-f560-4fbb-8bce-c25fb98f1e30"
    round: 2026-07-18-carrier-lanes
    title: "dev 이력 대비 살아있는 문서 staleness 감사·정정 (README 표면·배지, ADR-0001 SSOT 포인터, CLAUDE.md 릴리스 메커니즘, docs gate 확장)"
    status: done
  - id: fix/remote-verify-live-ref
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-carrier-lanes-fixes
    result: "@ 4673b58 (attempt-2, 위임 20260717T205101Z): 전용 임시 ref 라이브 증명 + persisted ref 신뢰 제거 + '.' 거부 + freeze parity. host 748 green·ruff clean, 적대 리뷰 2회전, criterion 9 잔여는 override-unmet 기록 + fix/shallow-ancestry-honesty 등록"
    accept: ["publication 증명이 라이브 원격 기반이다 — 정확한 upstream branch를 명시 refspec으로 fetch한 결과(FETCH_HEAD 또는 전용 임시 ref)를 증거 SHA로 사용하고, persisted refs/remotes/* 신뢰 경로가 remote.py verify와 common.py head_pushed 양쪽에서 제거된다", "RED-first 2건: (a) 원격 branch가 삭제된 상태, (b) branch가 fetch refspec에서 제외된 상태 — 양쪽 모두 gate가 거부한다 (실 bare-remote 테스트, 검증 clone과 삭제 주체 clone 분리)", "fetch 실패·branch 부재·빈 FETCH_HEAD는 전부 fail-closed이며 오류 메시지가 원인(부재 vs 네트워크)을 구분한다", "전체 테스트 스위트 green + ruff F401,F841 clean, 기존 publication gate 계약 테스트 무회귀", "freeze의 binding↔exposure 교차검증이 packet 게이트와 동등하다 — reviewers/mode 포함 전 필드 대조, exposure만 변조된 재렌더 게시를 거부한다 (RED; adversarial round-3 편입)", "증거 SHA는 공유 FETCH_HEAD가 아니라 전용 임시 ref(예: refs/waystone/verify-fetch-<pid>)로 원자 고정된다 — fetch refspec의 목적지로 임시 ref를 지정해 읽고 정리하며, 동시 fetch가 FETCH_HEAD를 덮어써도 증거가 오염되지 않는다 (RED: fetch 직후 FETCH_HEAD를 다른 SHA로 바꿔도 판정 불변)", "upstream remote가 '.'(로컬 repo 자신)이면 publication 증명을 거부한다 — 로컬 branch 추적은 원격 게시가 아니다 (RED: branch.X.remote=. 상태에서 게이트 거부 + 정직한 사유)", "freeze는 prepared pair 자체가 pr 모드임을 명시 검사한다(구 계약 복원 1줄) — 디렉터리 분리에만 의존하지 않는다", "shallow validator에서 ancestry 판정 불가는 '미포함'이 아니라 판정 불가로 정직하게 보고한다; RED bare-remote fixture는 init.defaultBranch와 무관하게 결정적이다(git init -b 핀)"]
    notes: "JW-GPT-001 (REAL major, 동적 재현으로 실제 false PASS 확인). 원칙: publication 증명은 라이브 원격에서. [attempt-2] attempt-1(위임 20260717T203026Z, discard됨)의 설계 방향은 전부 인수 — fetch_upstream_head(for-each-ref 기반 정확한 branch 해소, 명시 refspec, ls-remote --exit-code로 부재 vs 네트워크 구분), head_pushed(fetch=False) fail-closed, freeze _binding_matches_exposure, RED bare-remote fixture(clone 분리). 기각 사유 보강 4건: (1) 증거 SHA를 FETCH_HEAD(공유 가변 상태)에서 읽지 말고 fetch refspec 목적지에 전용 임시 ref를 지정해 원자 고정·정리 — 동시 fetch(IDE 자동 fetch 등) 경합 차단, (2) upstream remote '.'(로컬) 거부, (3) freeze의 명시 pr-mode pair 검사 1줄 복원(디렉터리 분리 + 벨트-앤-브레이스), (4) shallow에서 ancestry 판정 불가를 정직 보고 + fixture에 git init -b 핀. attempt-1 패치는 .waystone/delegations/20260717T203026Z-fix-remote-verify-live-ref/artifact/changes.patch 참조(단 sandbox에서 record dir 접근 불가할 수 있음 — notes의 설계 요약이 우선). 검증 재현은 tempdir 격리 필수."
    deps: ["fix/binding-narrative-digest"]
    scope: ["scripts/remote.py", "scripts/common.py", "scripts/review.py", "scripts/tests/run_tests.py"]
    title: "remote verify가 stale remote-tracking ref로 삭제된 upstream을 published로 오판 — 명시적 refspec fetch(FETCH_HEAD) 기반 라이브 원격 증명으로 재작성"
    status: done
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes"
  - id: fix/packet-digest-prompt-coverage
    result: "attempt-2 apply + main claim-게이트 하드닝(동일 커밋): digest≡prompt projection 결속, registry_fingerprint 제거. codex REJECT(prepare→claim 재정렬 창)는 main 검증으로 dispatch 영향 nil 판정 + 하드닝으로 잔여 창 폐쇄. 통합 730 green + ruff clean. 기록: .waystone/delegations/20260717T183011Z-fix-packet-digest-prompt-coverage"
    accept: ["plan 생성 후 tasks.yaml에서 notes·anchor·round·milestone 각각을 변조하면, 기존 manifest의 --expect-packet-sha로 호출된 delegate run이 claim record 생성 전에 거부된다 — 필드별 RED-first 4건", "digest 커버리지 원칙이 코드에 명시된다: implementer prompt에 렌더되는 registry 유래 필드 집합 = packet-core digest 필드 집합 (의도적 제외는 docstring에 열거된 carrier/retry 메타데이터만)", "registry_fingerprint가 정직해진다 — run 경로에서 실제 검증되거나 manifest에서 제거된다 (기록만 되고 소비 안 되는 상태 금지)", "전체 테스트 스위트 green + ruff F401,F841 clean, 기존 §4.1-4.3 delegate 계약 테스트 무회귀", "digest가 prompt 렌더와 결속된다 — canonicalization(json.dumps sort_keys)과 prompt 렌더(str())가 갈리는 클래스가 닫힌다: prompt-렌더 registry 필드는 scalar(str/None)로 강제되거나(비scalar면 plan/packet-build가 fail-loud) digest가 prompt가 보는 표현 그대로를 해싱한다. load_tasks 미검증 경로로도 mapping-typed notes/anchor가 우회하지 못한다", "canonicalization RED: notes(또는 anchor)를 YAML mapping으로 넣고 nested key 순서만 바꾼 tasks.yaml이 기존 --expect-packet-sha로 run 시 claim record 생성 전에 거부된다 (문자열→문자열 변조뿐 아니라 컨테이너 재정렬도)"]
    notes: "ATTEMPT-2 (2026-07-18): attempt-1 REJECT — load_tasks 미검증(common.py:1606)이라 notes/anchor mapping이 _render_prompt(str(dict), 삽입순서)와 _packet_core_digest(json.dumps sort_keys)를 갈라 우회 잔존. 근본 해소: prompt-렌더 필드를 scalar로 강제(비scalar fail-loud) 또는 digest를 prompt 렌더 표현에 결속. attempt-1의 필드 추가 + registry_fingerprint 제거는 유지. milestone/round/anchor/notes 필드별 RED 4건 + canonicalization RED 1건."
    scope: ["scripts/delegate.py", "scripts/tests/run_tests.py"]
    title: "packet-core digest가 prompt 렌더 필드(milestone/round/anchor/notes)를 누락 — 디스패치 내용에 영향 주는 모든 필드를 digest에 포함, 미소비 registry_fingerprint 정직화"
    status: "done"
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes"
  - id: fix/pending-corrupt-binding-honesty
    result: "attempt-2 apply(b858ec3): latest_round_request_binding 공유 헬퍼로 전 소비자(pending·ingest·improve) 통일, mkstemp+os.link crash-atomic 발행. codex 적대 리뷰 ACCEPT(no majors), 724→통합 730 green. 기록: .waystone/delegations/20260717T183010Z-fix-pending-corrupt-binding-honesty"
    accept: ["valid binding-1 + corrupt(빈 파일·부분 JSON) binding-2 + binding-1 target과 일치하는 feedback 상태에서 review pending이 해당 round를 pending(target unknown)으로 유지한다 — RED-first 계약 테스트", "최신 binding 후보는 파싱 이전에 파일명 시퀀스로 결정된다 — corrupt 후보를 건너뛰고 구 binding을 최신으로 승격하는 경로가 코드에서 제거된다", "sidecar 발행이 crash-atomic이다 — create-후-write 대신 tmp 완전 기록 후 exclusive publish, 기존 -N 시퀀스 명명과 동시 발행 배타성 보존", "전체 테스트 스위트 green + ruff F401,F841 clean, 기존 pending·binding 계약 테스트 무회귀", "latest-binding 해소(파일명 시퀀스로 최신 선택 → corrupt면 unknown)가 review.py의 단일 공유 헬퍼로 추출되고, pending_reviews와 improve.py의 review-target provenance 경로(_round_review_sidecars/_review_target_provenance, 984-985 max 포함)가 모두 그 헬퍼를 경유한다 — corrupt 최신 binding을 구 binding으로 승격하는 경로가 전 소비자에서 사라진다", "improve projection RED: valid binding-1 + corrupt binding-2 + 일치 feedback 상태에서 improve의 review-target provenance가 explicit로 구 target을 승격하지 않는다(unknown/미승격)"]
    notes: "JW-GPT-003 (REAL major). 원칙: 최신 binding 후보를 파일명 시퀀스로 먼저 결정, corrupt면 pending/unknown 유지. sidecar는 tmp 완전 기록 후 os.link 배타 발행으로 crash-atomic. ATTEMPT-2 (2026-07-18): attempt-1 REJECT — improve.py:984-985(_round_review_sidecars 1025)가 pending_reviews와 동일 결함(파싱 성공 집합에서 max로 구 binding 승격). 근본 해소: latest-binding 해소를 review.py 단일 공유 헬퍼로 추출해 pending_reviews·improve 양쪽이 경유(중복 수정 금지). os.link 발행 방식은 유지."
    scope: ["scripts/review.py", "scripts/tests/run_tests.py"]
    title: "pending 파생이 corrupt 최신 binding을 건너뛰고 구 binding+구 feedback으로 완료 처리 — 시퀀스 우선 최신 판정(corrupt면 pending 유지) + sidecar crash-atomic 발행"
    status: "done"
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes"
  - id: fix/binding-narrative-digest
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-carrier-lanes-fixes
    result: "@ 874c4c3 (attempt-3, 위임 20260717T200543Z): narrative digest 결속 + freeze/packet 재렌더 게이트 + 양측-digestless 한정 라벨 폴백 + improve 투영 보존. host 738 green·ruff clean, 적대 리뷰 3회전(freeze exposure-parity는 remote-verify-live-ref로 편입)"
    accept: ["binding contract에 검증된 narrative의 canonical digest가 포함된다 — narrative만 바뀐 재-prepare는 새 sidecar를 발행하고 해당 round가 pending으로 재개된다 (RED-first: prepare A → feedback ingest → prepare B 같은 target → pending 재개)", "freeze는 stored narrative digest를 최신 binding과 대조한 뒤에만 게시한다 — 불일치는 fail-loud", "digest 없는 기존(legacy) sidecar는 조용히 통과되지 않는다 — 정직한 unknown/재발행 유도 경로로 처리", "전체 테스트 스위트 green + ruff F401,F841 clean", "packet publication 게이트가 narrative 결속을 검증한다 — round exposure + stored narrative에서 request를 재렌더해 게시 대상 request.md와 대조(freeze와 동일 원리)하거나 동등한 fail-loud 검증으로, prepare 후 request.md narrative만 변조된 packet을 게이트가 거부한다 (RED 계약 테스트)", "(기존 legacy 항목 개정) digest 없는 legacy binding의 완료 판정은 구 계약(review_target_matches)으로 폴백하되 coverage reason에 legacy-pre-digest 라벨을 명시한다 — 재발행 유도는 재발행이 실제 가능한 현행 round에만, publication 게이트(freeze·packet verify)는 digest를 계속 하드 요구, 과거 라운드가 실행 불가능한 안내와 함께 영구 pending으로 남지 않는다 (RED: digestless binding + target-일치 feedback → pending 미등재 + 라벨 노출)", "legacy 폴백은 양측 digestless일 때만 적용된다 — binding에 digest가 없어도 feedback에 stamped digest가 있으면(post-upgrade 증거) 폴백을 거부하고 pending 유지 (RED: prepare A→ingest→prepare B 후 binding-2에서 narrative_digest만 제거해도 완료로 오판되지 않는다)", "improve의 review binding 재구성이 narrative_digest를 보존한다 — digest-bound binding에 대한 stamped digest mismatch가 improve 투영에서 legacy-pre-digest로 오보되지 않는다", "legacy-pre-digest 라벨은 reviewer coverage 사유와 독립적으로 항상 노출된다 — unconfigured/미식별 reviewer의 기존 coverage reason이 라벨을 밀어내도 legacy 완료가 무라벨로 지나가지 않는다 (RED: unconfigured reviewer + digestless binding 실파일 흐름)"]
    notes: "JW-GPT-004 (REAL major). binding contract에 canonical narrative digest — 재-prepare 시 pending 재개, freeze/packet 게이트 digest 대조, legacy는 라벨 폴백. [attempt-3] attempt-2 설계(digest 결속·strict latest 헬퍼·packet 재렌더 byte 대조·라벨 폴백 방향)는 인수 — 기각은 legacy 폴백 정직성 3건: (1) 폴백은 양측 digestless일 때만 — binding digest 부재 + feedback stamped digest 존재(post-upgrade 증거)면 폴백 거부·pending (digest-strip 위장 차단), (2) improve.py _review_binding 재구성이 narrative_digest 탈락 — 보존해 mismatch가 legacy로 오보되지 않게 (scope에 improve.py 추가됨), (3) legacy-pre-digest 라벨이 reviewer coverage reason에 밀려 소실 — 독립 노출(unconfigured reviewer RED 실파일 흐름 포함). template 재렌더 미결속 우려는 기존 확정 설계(재렌더가 무결성, template은 커밋 추적)로 반박 — 변경 금지. ingest 도장 race는 fix/reply-narrative-echo 별도 task — 변경 금지. 검증 재현은 tempdir 격리 필수."
    deps: ["fix/pending-corrupt-binding-honesty"]
    scope: ["scripts/review.py", "scripts/tests/run_tests.py", "scripts/improve.py"]
    title: "binding contract에 narrative/request digest 부재 — 동일 target 재-prepare가 구 sidecar·구 feedback 재사용. narrative digest를 contract에 포함해 재발행 시 pending 재개"
    status: done
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes"
  - id: fix/probe-proof-machine-scope
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-carrier-lanes-fixes
    result: "@ 874c4c3 (attempt-3, 위임 20260717T200543Z): 마커 미커밋 티어 이동 + untracked/내용 검증 + 복원적 self-ignore 통일 + barrier 동시성 계약. host 732 green·ruff clean, 적대 리뷰 3회전(잔여는 진단 폴리시로 minor 분리)"
    accept: ["codex 프로브 증명이 커밋 추적 파일(.waystone.yml)에 더 이상 기록되지 않는다 — 저장 위치가 미커밋 티어({root}/.waystone, self-ignored)로 이동하고 읽기·쓰기·진단 안내가 전부 새 위치를 가리킨다", "RED-first: .waystone.yml에 legacy codex_runner_verified: true가 커밋돼 있어도 로컬 마커가 없으면 프로브가 실행된다 — legacy 키는 조용히 수용되지 않고 무시+제거 안내를 출력한다", "전체 테스트 스위트 green + ruff F401,F841 clean, 프로브 1회 실행(probe-once) 계약 무회귀", "{root}/.waystone self-ignore는 복원적·원자적이다 — .gitignore가 없거나 내용이 '*' 전체 무시를 상실한 상태(빈 파일 포함)에서도 상태 디렉터리 ensure 경로가 canonical ignore를 원자적으로 복구해 마커가 git 추적 가능해지지 않는다 (RED: 빈 .waystone/.gitignore 상태에서 마커 기록 후 git check-ignore가 마커를 무시로 판정)", "legacy 키 제거 안내는 디스크 config를 실제로 읽은 로드 경로에서만 발화한다 — source 미상 normalize(statusline 표시·과거 ref blob 정책 투영)는 존재하지 않는 로컬 경로를 안내 문구에 지어내지 않는다", "마커 없이 동시 시작한 두 runner 경로에서 프로브가 정확히 1회 실행된다 — lock 하 마커 재확인 동시성 계약 테스트", "로컬 마커는 git 추적 상태가 아닐 때만 증명으로 인정된다 — 커밋/스테이징돼 전파된 마커는 무시하고 untrack 제거 안내를 출력한 뒤 프로브를 실행한다 (RED: 마커가 커밋된 체크아웃에서 로컬 증명 없이 프로브 실행 + 안내 출력)", "마커 내용이 계약값(verified)이 아니면 증명으로 인정하지 않는다 — 빈/손상 마커는 무시+안내 후 재프로브하고 성공 시 계약값으로 원자 재기록한다", "self-ignore 기록은 단일 복원적·원자적 헬퍼로 통일된다 — legacy migration 경로(_ensure_project_state_raw)도 같은 헬퍼를 경유하며 symlink .gitignore를 수용하지 않는다", "동시성 계약 테스트가 lock 하 재확인 자체를 고정한다 — 두 runner 모두 잠금 전 1차 마커 확인을 부재 상태로 통과한 것이 보장된 뒤에 첫 프로브가 완료된다 (재확인 제거 구현이면 RED)"]
    notes: "리뷰어 open question 1 승격 (REAL major, 상태 격리 위반). proof 저장을 커밋 추적 .waystone.yml에서 미커밋 티어 {root}/.waystone(self-ignored, per-checkout) 마커로 이동, legacy 키는 무시+제거 안내. [attempt-3] attempt-2는 마커 이동·legacy fail-loud·복원적 self-ignore·source-aware 안내를 정확히 구현(전부 인수) — 기각은 round-2 적대 리뷰의 신규 4건: (1) 마커 read-side가 git 추적 상태 무검증 — git ls-files 류로 추적된 마커는 증명 불인정(무시+untrack 안내+프로브, legacy 키 처리와 동일 패턴), (2) 마커 내용 무검증 — 계약값 아닌 파일은 불인정+재프로브, (3) common.py _ensure_project_state_raw(migration)가 별도 self-ignore 구현(symlink 추종 is_file + 비원자 write_text) — 단일 헬퍼로 통일, (4) 동시성 테스트가 두 worker 모두 잠금 전 1차 확인을 부재로 통과했음을 보장 안 함 — 1차 확인 지점 계측으로 재확인 제거 시 RED 되게. 주의: statusline(waystone.py)의 sourceless normalize는 의도된 무발화 표면(표시 표면 stderr 노이즈 방지) — 변경 금지. 검증 재현은 tempdir 격리 필수."
    deps: ["fix/packet-digest-prompt-coverage"]
    scope: ["scripts/delegate.py", "scripts/common.py", "scripts/tests/run_tests.py"]
    title: "codex 프로브 증명이 커밋 추적 .waystone.yml에 저장돼 머신 간 전파(상태 격리 위반) — 미커밋 티어({root}/.waystone)로 이동"
    status: done
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes"
  - id: docs/release-single-writer-assumption
    notes: "release-to-main.sh에 단일 사용자·단일 프로세스 전제 주석 명시 (SSOT §3 근거, lease 기계는 speculative라 배제). 리뷰어 open question 2 해소."
    title: "release 스크립트의 단일 사용자·단일 프로세스 동시성 전제를 명시 (lease 기계 도입 대신 제한 문서화 — SSOT §3)"
    status: "done"
    severity: "minor"
    origin: "review-2026-07-18-carrier-lanes"
  - id: fix/reply-narrative-echo
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-generation-binding
    result: "@ cc5ae84 (attempt-3, 위임 20260718T081828Z): request 자기 digest 노출(sentinel 순환 해소) + 에코 필수(echo-era) + generation 일관 도장 + 읽기 시점 재대조. 3회전(적대 2회) 인수, live 777 green — JW-GPT-004A 폐쇄"
    scope: ["scripts/review.py", "scripts/tests/run_tests.py", "templates", "skills/round"]
    accept: ["request 렌더가 rendered_request_digest를 리뷰어가 복사 가능한 형태로 노출한다 — 회신 헤더 안내 블록(model/effort/review-target)에 request-digest 키가 추가된다", "reply 구조화 헤더 파서가 request-digest를 인식하고, ingest는 에코된 digest를 최신 binding의 rendered_request_digest와 대조해 도장한다 — 에코 존재 시 그것이 도장 근거(현재-binding 재도장 race 차단), 불일치는 완료 불인정 + 정직한 사유", "RED-first: prepare A → A의 digest를 에코한 회신 → 같은 target reprepare B → 회신 ingest → round가 pending을 유지한다(B 완료 오판 없음 — JW-GPT-004 메커니즘 A의 재현이 닫힘)", "에코 부재 회신(구 형식)은 기존 ingest-time 도장 경로로 폴백하되 커버리지 사유에 에코 부재가 명시된다 — 조용한 동등 취급 금지", "전체 테스트 스위트 green + ruff F401,F841 clean — reply 파서·digest·publication 계약 무회귀", "improve 투영이 reply receipt의 rendered-request digest·mismatch 필드를 보존한다 — render-only reprepare 후 pending과 reviews.jsonl 투영이 불일치하지 않는다 (A-lane 적대 리뷰 minor 편입)", "echo-era 라운드(최신 binding이 v2)의 에코 부재 회신은 폴백 없이 완료 불가다 — 정직한 안내(request의 digest 라인을 에코해 재제출)와 함께 pending 유지; ingest-time 도장 폴백은 legacy 라운드(v1 binding) 한정 (RED: v2 라운드 회신에서 request-digest 라인 제거 → 완료로 오판되지 않음)", "에코 존재 시 receipt는 에코가 명명하는 generation으로 일관 도장된다 — 라운드의 불변 sidecar들에서 에코 digest를 조회해 해당 generation의 narrative digest를 함께 기록하고, 현재 binding 값과 혼합하지 않는다; stale generation(구 sidecar 일치)과 미상 digest는 구분된 사유로 보고 (RED: stale 에코 A ingest 후 receipt가 A generation으로 일관)", "coverage 필드가 없는 구(echo 이전) receipt는 무조건 match로 판독되지 않는다 — 에코 부재 provenance로 정직 강등", "self-digest 라인 식별은 회신/렌더의 헤더 블록 한정이다 — 서사 본문의 동일 문자열(request-digest:)이 오탐되지 않는다 (RED: 서사에 해당 문자열 포함 라운드가 정상 동작)", "no-echo provenance가 후속 reprepare 이후에도 receipt에 유지된다 — stale 에코와 구분 가능", "receipt 판독은 저장된 사유를 신뢰하지 않고 읽기 시점에 저장된 에코 digest를 현재 최신 binding과 재대조한다 — A 복원 재-prepare 후 stale였던 receipt가 자동으로 match로 회복된다 (RED); 저장 사유는 주석으로만 유지", "재제출 안내는 복사 가능한 digest 값을 인쇄하지 않는다 — 리뷰어가 실제로 본 request의 request-digest 라인을 에코하라는 출처 지시만 제공 (안내를 따르는 것이 race를 재현할 수 없다)"]
    notes: "JW-GPT-004 메커니즘 A. [attempt-3 — 최종, 기계적 2건 한정, 신규 major 시 escalation] attempt-2(위임 20260718T075014Z, discard)에서 인수: sentinel 순환·파서·에코-era 폴백 한정(legacy=v1 라운드만)·sidecar 조회 generation 일관 도장·stale/미상 구분·구 receipt 강등·헤더 한정·no-echo provenance. 남은 2건만: (1) [major] 리더의 저장 stale 사유 단락 제거 — 읽기 시점에 저장 에코 digest vs 현재 최신 binding 재대조(projection 재계산 원칙, triage-discipline ruling과 동일), 저장 사유는 주석, RED: A→B→A에코 ingest(stale)→A 복원 재-prepare→자동 match 회복, (2) [minor] 재제출 안내에서 digest 값 인쇄 제거 — 출처 지시만. 전환일 다운그레이드 재발견은 문서화된 잔여로 반박됨 — 변경 금지. tempdir 격리·env -u FORCE_COLOR."
    title: "리뷰 회신이 검토한 narrative를 자기증언하지 않아 ingest가 현재 binding digest를 도장 — prepare(A)→리뷰 중 prepare(B)→A회신 ingest 시 B 완료 오판. request 렌더에 digest 노출 + reply 구조화 헤더에 request-digest 에코 + ingest 대조 (reply 계약 변경 — 사용자 ruling 필요)"
    status: done
    severity: "major"
    deps: ["fix/binding-narrative-digest", "fix/binding-schema-v2-digests"]
  - id: chore/legacy-name-residue
    notes: "조사(2026-07-18): live 표면 잔재 — scripts/{common,delegate,improve,review}.py·run_tests.py의 JW_REPORT.yaml 계약, templates/delegate-prompt.md, hooks/scripts/{resume_snapshot.sh,session_context.sh,tasks_read_nudge.py}, skills/{init,review}/SKILL.md. 경계: 역사 기록(docs/reviews verbatim 아카이브·PROGRESS.md·tasks.yaml done 항목의 JW-GPT-N provenance 참조)은 보존 — 사용자 지시의 '마이그레이션 불요'는 런타임 호환 폐기이지 이력 개서 아님. skills/review의 finding ID 스킴(JW-GPT 접두)은 신규 라운드부터 새 접두(WS-GPT 등)로 전환. JW_REPORT.yaml → WAYSTONE_REPORT.yaml 류로 rename(계약·테스트·프롬프트 일괄). dev_docs/delegate_readme.md는 참고 원본이라 보존 또는 삭제는 사용자 판단."
    title: "구명 잔재 전면 제거: JW_REPORT.yaml 등 JW·jahns-workflow 이름이 남은 표면을 rg 전수로 청소 (마이그레이션 호환 불요 — 사용자 지시 2026-07-18). delegate.py 계약·in-flight lane과 겹치므로 lane 머지 후 실행"
    status: "pending"
    severity: "minor"
    deps: ["fix/probe-proof-machine-scope", "fix/binding-narrative-digest"]
  - id: docs/readme-delegate-fold
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-carrier-lanes-fixes
    result: "README delegate 섹션 재작성 @ 07bab60 — 리드+details fold, docs gate·730 green"
    title: "README delegate 섹션 재작성 — dev_docs/delegate_readme.md(codex 해설, 구버전 기준)를 참조 스타일로 최신 dev 동작에 맞게 재작성, 상세 메커니즘은 details fold로 접어 배치 (사용자 지시 2026-07-18, main 직접 — docs 작업)"
    status: done
    severity: "minor"
  - id: fix/hook-matrix-color-env
    scope: ["scripts/tests/run_tests.py"]
    title: "hook matrix 테스트의 uv cache dir 파싱이 FORCE_COLOR 환경에서 ANSI 오염으로 실패 — subprocess env에서 색상 강제 변수 중화(또는 NO_COLOR 부여)로 환경 무관하게 (2026-07-18 실측: FORCE_COLOR=3 세션에서 main tree도 동일 실패, lane 회귀 아님)"
    status: "pending"
    severity: "minor"
    deps: ["fix/probe-proof-machine-scope", "fix/binding-narrative-digest"]
  - id: fix/marker-diagnostics-polish
    title: "probe 마커 진단 폴리시: 추적 마커+워킹트리 삭제 상태의 1회차 안내 공백, staged 무효 마커 시 git rm --cached -f 안내 보강, lock 하 재확인 경합 창의 안내 억제 — 전부 fail-open 아님(프로브는 항상 실행), 안내 품질만 (adversarial round-3, main 판정 minor)"
    status: "pending"
    severity: "minor"
    deps: ["fix/probe-proof-machine-scope"]
  - id: chore/pre-header-feedback-settlement
    title: "구조화 헤더(0.10) 이전 형식의 역사 feedback 3라운드가 완료 판정 불가로 영구 pending — 패치 전부터 동일(기존 부채). 역사 라운드 정착(settle) 방식 결정 필요: 아카이브 마커 vs 재-ingest vs pending 제외 규칙"
    status: "pending"
    severity: "minor"
  - id: fix/shallow-ancestry-honesty
    scope: ["scripts/common.py", "scripts/tests/run_tests.py"]
    title: "shallow 위상에서 merge-base rc=1이 판정 불가일 수 있음에도 확정 '미포함'으로 보고(fail-closed 오진단) — is-shallow-repository 감지 시 rc=1을 unverifiable로 정직 강등; 임시 verify-fetch ref의 인터럽트 잔류 sweep 포함 (adversarial V-round-2, main 판정 minor)"
    status: "pending"
    severity: "minor"
    deps: ["fix/remote-verify-live-ref"]
  - id: fix/binding-schema-v2-digests
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-generation-binding
    result: "@ a8f208a (attempt-5=인수된 attempt-4의 hash-동일 재적용, 위임 20260718T065659Z): binding-2 이중 digest 필수 + 날짜 cutoff 외부 앵커(> 2026-07-18) + 게이트 rendered digest 대조 + improve v2 join. 5회전(적대 3회) 인수, live 768 green"
    notes: "JW-GPT-006 + 004-B. [attempt-5 — base drift 재적용 전용, 설계 변경 금지] attempt-4(위임 20260718T064517Z)는 verdict apply 인수됐으나 병렬 F-lane 병합(84c1293이 run_tests.py 수정)으로 patch 적용 불가 — 유일한 임무는 attempt-4 패치(.waystone/delegations/20260718T064517Z-fix-binding-schema-v2-digests/artifact/changes.patch — 이번엔 record dir이 sandbox에서 안 보일 수 있으니 무시 가능)와 동일한 결과를 현재 base 위에 재현하는 것. 내용: attempt-3 본체(v1/v2 판별·이중 digest·게이트 rendered digest 대조·경계 > 2026-07-18·packet 날짜 엄격성·improve v2 join·freeze byte-동일·round close mint 검증) + attempt-4 기계 4항목(PR 리더 비실재 날짜 corrupt(2026-99-99 RED), mint 검증을 _round_has_existing_closeout 게이트로 신규 한정(익일 재close 허용 RED), round._current_date 주입 clock 결정론, 전환일 잔여 주석 문서화+정착 이관 명시). 전 accept 기준 동일 유지. tempdir 격리·env -u FORCE_COLOR."
    accept: ["digest-capable binding은 새 schema(waystone-round-request-binding-2)로 발행되고 narrative_digest·rendered_request_digest가 필수다 — v2에서 어느 하나라도 부재/무효면 corrupt로 fail-loud", "binding-1은 genuine legacy 읽기 전용으로만 수용된다 — 신규 발행은 항상 v2, digest-capable round의 digestless binding은 ingest가 거부한다 (RED: 게시 후 ingest 전에 binding에서 digest만 제거 → 완료로 오판되지 않고 legacy-pre-digest 위장 불가)", "publication 게이트와 freeze는 게시 내용을 stored rendered_request_digest와 대조한다 — template 정적 텍스트나 exposure의 미결속 필드(project/branch)만 변조해 기존 feedback이 완료를 유지한 채 다른 내용이 게시될 수 없다 (RED: template 또는 exposure 변조 후 게이트 거부)", "improve 투영이 v2 digest 필드를 보존하고 v1/v2 provenance를 정직하게 구분한다", "전체 테스트 스위트 green + ruff F401,F841 clean — 기존 digest 계약(narrative-only 재-prepare pending 재개, 도장-후-strip 차단, 재렌더 대조) 무회귀", "digest-capability는 sidecar 내부 필드만으로 결정되지 않는다 — 외부 앵커(권장: round id 날짜 cutoff 상수 — digest 시대 이후 round의 v1 binding은 corrupt; 대안: closeout exposure의 결속 필드)로 v1 legacy 수용 범위를 고정해, schema 문자열 다운그레이드+digest 제거 2-필드 수술로도 legacy 위장이 불가하다 (RED: v2 게시 후 binding을 v1로 다운그레이드+digest 제거 → 완료로 오판되지 않음)", "PR-mode improve 투영도 request binding v2의 digest·v1/v2 provenance를 보존한다 — freeze binding만으로 투영하지 않는다", "freeze가 게시하는 최종 bytes는 digest 검증 대상과 동일하다 — 검증 후 변형(rstrip 등) 금지 또는 게시 형태 자체를 digest 대상으로 통일 (RED: 검증-후-변형이 있으면 실패)", "strip-계열 RED가 다운그레이드 위장 케이스를 실제로 커버하며, 패치 전 코드에서의 실패가 우발(KeyError)이 아니라 구 동작(위장 성공)의 입증이다", "digest 시대 경계는 2026-07-18을 legacy로 포함한다 — 0.10.0이 발행한 same-day 정품 v1(현 repo의 2026-07-18-carrier-lanes*-request.binding.json)이 corrupt로 소급되지 않는다 (경계: cutoff보다 이후만 v2 강제, RED: 실존 same-day v1 형태 fixture가 legacy로 판독됨)", "round id의 날짜부가 실제 달력 날짜로 파싱되지 않으면 binding 판독은 corrupt다 — 비실재 날짜(2026-99-99 등)의 legacy fail-open 강등 금지 (RED)", "round 신설 표면(round close)이 round id의 날짜부가 현재 날짜와 일치함을 검증한다 — 백데이트 신규 round 차단; 전체 라운드 아티팩트 일괄 위조는 위협모델 밖임을 앵커 코드 주석에 문서화", "PR 경로도 날짜 엄격성 패리티 — malformed round id가 read_pr_freeze_binding·_round_requires_digest_binding 경로에서 legacy로 fail-open하지 않는다 (RED: 2026-99-99-r1 PR round)", "mint-date 검증은 신규 round에만 적용된다 — 기존 exposure/PROGRESS가 있는 round id의 round close 재실행(익일 확장 포함)은 거부되지 않는다 (RED: 기존 round 익일 재close 성공)", "close 계열 테스트는 clock 결정론적이다 — 자정 경합으로 무관 테스트가 실패하지 않는다", "전환일(≤cutoff) round의 v2→v1 다운그레이드 잔여는 앵커 코드 주석에 유한 위험 창으로 문서화되고, 역사 라운드 정착 task로 이관이 명시된다"]
    scope: ["scripts/review.py", "scripts/improve.py", "scripts/tests/run_tests.py", "scripts/round.py"]
    title: "binding schema v2 분리 — narrative_digest·rendered_request_digest를 필수 필드로 하는 waystone-round-request-binding-2 도입, binding-1은 genuine legacy 전용(digest 부재=corrupt는 v2만), digest-capable round의 digestless binding은 ingest가 거부, publication/freeze는 stored rendered digest 대조로 template·exposure 미결속 필드(project/branch)까지 간접 결속 (JW-GPT-006 + JW-GPT-004 메커니즘 B)"
    status: done
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes-fixes"
  - id: fix/reprepare-generation-atomicity
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-generation-binding
    result: "@ 79f241e (attempt-1, 위임 20260718T071128Z): binding 선발행 fail-closed 순서 + pending 재현성 확인 + render-only 무효화. RED-first, live 771 green"
    accept: ["reprepare 순서가 fail-closed다 — 새 binding(v2)을 먼저 발행해 구 feedback을 무효화한 뒤 request·stored narrative projection을 갱신한다(또는 동등한 원자적 generation 전환) — 시퀀스 어느 지점에서 프로세스가 중단돼도 구 binding+구 feedback 조합이 완료로 남지 않는다", "RED-first: prepare A → ingest A(완료) → reprepare B 중 request 기록 직후 강제 종료 → pending에 해당 round가 반드시 남는다; stored narrative 기록 직후 종료도 별도 계약으로 고정", "pending 완료 판정이 최신 binding에서 request·narrative의 재현성을 확인한다 — 디스크 request가 최신 binding과 불일치하면 완료로 숨기지 않고 pending 유지(정직한 사유 노출)", "전체 테스트 스위트 green + ruff F401,F841 clean — schema-v2·digest·publication 계약(rendered digest 대조·재-prepare pending 재개·legacy 처분) 무회귀"]
    scope: ["scripts/review.py", "scripts/tests/run_tests.py"]
    title: "reprepare generation(request+narrative+binding)이 비원자 — request 기록 직후 crash 시 구 binding+구 feedback이 완료를 유지한 채 디스크에 B request 잔존(fail-open). 새 binding 선발행으로 구 feedback 무효화 후 projection 갱신 + pending이 request/narrative의 최신 binding 재현성 확인 (RED: request write 직후 강제 종료 → pending 잔존) (JW-GPT-005)"
    status: done
    severity: "major"
    deps: ["fix/binding-schema-v2-digests"]
    origin: "review-2026-07-18-carrier-lanes-fixes"
  - id: fix/probe-proof-runtime-fingerprint
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-18-generation-binding
    result: "@ 84c1293 (attempt-4, 위임 20260718T063139Z): fingerprint 결속 JSON 마커(host identity·resolved+stat·stdout version·sandbox 관측·mount) + exact-match 생략 + resolved 경로 실행 통일 + codex 부재 CI-동등 스위트 green. 4회전 인수"
    notes: "Q1 잔여. [attempt-4 — 설계 재량 0의 기계적 closeout, 신규 major 시 즉시 중단·escalation] attempt-3(위임 20260718T060555Z, discard)에서 계약 본체 전부 인수: versioned JSON·machine-id/IOPlatformUUID(UUID 형식 검증)·resolved 경로 실행(runner·probe·--version)·등가 축 결정론화(stdout+stat, stderr 기록 전용)·sentinel fail-closed·mount 대상 일치·lock 안내·legacy 처리. 남은 기계적 4건만: (1) [major] fingerprint 획득을 mock하지 않은 신규 테스트가 codex 없는 CI(ci.yml test job)에서 실패 — 전 테스트가 실환경 codex 비의존이 되도록 mock, RED는 PATH 제거 환경 스위트 green, (2) verifier(_run_codex_verifier)도 resolved 경로로, (3) machine-id 정규형 32 lowercase hex + all-zero 거부, (4) stderr 소거 잔여 주석 문서화. 그 외 변경 금지. 검증 재현 tempdir 격리·env -u FORCE_COLOR."
    accept: ["프로브 증명 마커가 versioned JSON 계약이 된다 — proof schema 버전, resolved codex 실행 파일 경로·버전, platform/kernel, sandbox 정책 식별자, worktree cache mount/device identity를 기록", "프로브 생략은 현재 환경 fingerprint와 exact-match일 때만이다 — 불일치는 무시+어느 축이 달라졌는지 정직한 안내 후 재프로브 (RED: 타 환경 fingerprint로 기록된 마커·codex 버전만 달라진 마커 각각에서 프로브 실행)", "legacy 고정 문자열(verified) 마커는 조용히 수용되지 않는다 — 무시+안내 후 재프로브하고 성공 시 신 계약으로 원자 재기록 (RED)", "기존 계약 무회귀: untracked-만 인정, probe-once 동시성(lock 하 재확인), 복원적·원자적 self-ignore, legacy config 키 안내 전부 유지", "전체 테스트 스위트 green + ruff F401,F841 clean", "호스트 정체성이 hostname을 넘는다 — Linux /etc/machine-id, macOS IOPlatformUUID(또는 동등 안정 식별자)를 기록하고 획득 실패는 fail-closed (RED: machine-id만 다른 마커에서 재프로브)", "codex 버전 축이 stdout 세탁에 견딘다 — --version의 stdout+stderr를 결합 기록하고 resolved 실행 파일의 stat 정체성(size·mtime)을 함께 결속 (RED: stdout 배너 고정 shim 아래 바이너리 교체 → 재프로브)", "sandbox 축은 관측 가능한 것만 정직하게 — 호출 계약 라벨과 별도로 관측 가능한 호스트 sandbox 상태(Linux: /sys/kernel/security/lsm 존재 시)를 best-effort 기록하고, 필드 명명·주석이 '런타임 정책 전체 결속'을 과대 주장하지 않는다", "mount 축에 readonly 상태(statvfs flag)를 포함하고 축의 탐지 범위(장치/fsid 기반 — bind mount 재구성은 비탐지)를 코드 주석으로 문서화한다", "lock 하 재확인에서 발견된 불일치도 축 안내를 출력한다 — 무음 교체 금지", "runner·probe·--version 실행이 전부 fingerprint의 resolved 절대경로를 사용한다 — bare codex의 PATH 재조회 금지 (체크-실행 간 재타게팅 미결속 차단)", "fingerprint 등가 비교 축은 런 내 결정론적이다 — version 비교는 --version stdout + resolved 바이너리 stat(size·mtime_ns)로 하고 stderr는 진단용 기록만(등가 제외); 동적 stderr(타임스탬프·PID)를 내는 래퍼에서도 probe 성공이 기록에 도달하고 위임이 성공한다 (RED: 동적 stderr shim 아래 위임 성공 + 마커 기록)", "Linux machine-id의 sentinel 값(빈 값·공백·uninitialized)은 획득 실패로 fail-closed; macOS IOPlatformUUID는 UUID 형식 검증을 통과한 값만 수용 (RED)", "probe가 실제 쓰기를 수행하는 경로와 fingerprint의 mount 축이 동일 대상을 가리킨다 — 다른 mount를 검증하고 다른 mount의 증명을 기록하는 조합 불가", "테스트 스위트가 codex 부재 환경에서 green이다 — fingerprint 획득(shutil.which·resolve·stat·--version)을 실환경에 의존하는 테스트가 없도록 mock/주입 (RED: PATH에서 codex를 제거한 환경에서 전체 스위트 실행 — CI test job 동등)", "verifier의 codex 실행도 fingerprint의 resolved 절대경로로 통일된다 — bare codex 실행 표면 0", "Linux machine-id는 정규형(32자 lowercase hex)만 수용하고 all-zero를 거부한다; stderr-only version이 등가에서 소거되는 잔여(정적 shim 아래 child 교체 비탐지)는 코드 주석으로 문서화된다"]
    scope: ["scripts/delegate.py", "scripts/common.py", "scripts/tests/run_tests.py"]
    title: "probe proof 마커를 machine/runtime fingerprint 결속 versioned JSON 계약으로 — resolved codex path·version, platform/kernel, sandbox policy, worktree cache mount identity를 기록하고 exact-match일 때만 프로브 생략 (NFS/checkout 복사 전파·runtime drift 차단; Q1 잔여)"
    status: done
    severity: "major"
    origin: "review-2026-07-18-carrier-lanes-fixes"
  - id: fix/receipt-read-time-rederive
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-19-evidence-authority
    result: "@ 4af545d (attempt-3, 위임 20260718T145517Z): body 재파생 권위 + 세 투영 격리·패리티 + bounded 리더 + taxonomy 구분. live 793 green — JW-GPT-007 폐쇄"
    notes: "JW-GPT-007. [attempt-3 — 재량 0 기계적 closeout, 신규 major 시 escalation] attempt-2(위임 20260718T141654Z, discard) 인수분: body 재파생 권위·legacy 경로 cache 비신뢰·review.py 격리·bounded 리더·sidecar 부재 unknown(요청 축)·improve 공유. 남은 6항목: (1) overlay round_id 사전 검증(overlay.py:587 인근 — '/' row가 전체 중단), (2) overlay가 digest verdict 반영(625 — stale을 recognized로 계상 금지), (3) envelope 손상 vs cache/body 불일치 사유 구분(review.py:727), (4) sidecar 부재 시 narrative 축 unknown(798 — mismatch 오보), (5) 16KiB 경계·CRLF 산술(221), (6) request 파일명 metachar 검증(851). 그 외 변경 금지. tempdir 격리·env -u FORCE_COLOR."
    accept: ["receipt 판독의 권위는 verbatim body 재파생이다 — 읽기 경로가 body에 parse_review_reply_header를 재적용하고 body가 선언한 request-digest를 불변 request sidecar generation에 resolve한다 (RED: reply-metadata-json의 digest 값만 B로 편집 → body 재파생과 불일치로 corrupt/pending, 완료 오판 없음)", "reply-metadata-json은 cache/진단 전용으로 강등된다 — 완료 판정에 필요한 어떤 값도 cache 단독으로 신뢰되지 않는다", "body 범위 식별이 결정론적이다 — 헤더 separator 계약 기반이며 손상(separator 부재/중복 등)은 정직한 unknown으로 강등", "improve 투영이 동일 재파생 경로를 공유해 pending과 불일치하지 않는다", "전체 테스트 스위트 green + ruff F401,F841 clean — 기존 receipt/digest/에코 계약 무회귀", "legacy(no-echo) 완료 경로도 cache 키를 신뢰하지 않는다 — coverage 사유는 body 재파생(에코 부재)과 binding v1-여부에서 재도출되며, cache에 키를 추가하는 편집만으로 역사 receipt가 완료로 승격되지 않는다 (RED)", "손상 receipt는 격리된다 — 저장된 round:/metadata 값은 어떤 파일시스템 연산(glob 포함)에 도달하기 전에 검증되고, 한 receipt의 손상('/' round 값 포함)이 전체 pending/improve 투영을 중단시키지 않는다 (RED)", "digest-도장 receipt의 named sidecar 부재/손상은 request-digest-unknown 계열의 정직한 사유다 — receipt-corrupt로 오분류되지 않는다 (RED)", "리더는 bounded다 — body 재파생은 헤더 계약 범위의 한계 읽기로 수행되고, 임의 크기 회신이 pending/improve 스캔을 마비시키지 않는다 (기존 bounded 리더 계약 복원)", "overlay 소비자도 재파생 경로와 request 디렉터리 해석(PR-mode 포함)을 공유한다 — 정상 보존된 PR 회신이 corrupt로 재투영되지 않는다", "overlay 투영도 격리·verdict 패리티를 갖는다 — round_id 등 파일시스템 도달 값은 사전 검증(한 손상 row가 전체 overlay 중단 불가), digest verdict(stale/unknown)가 recognized-feedback 계상에 반영된다 (RED 2건)", "사유 taxonomy가 구분된다 — envelope 손상 vs cache/body 불일치 vs named sidecar 부재(narrative 축 포함: mismatch가 아니라 unknown)가 각기 다른 정직한 사유로 보고된다", "헤더 경계 산술이 정확하다 — 16KiB 정확 경계·CRLF에서 과소/과대 계상 없음; request 파일명 유래 round id도 glob 도달 전 검증된다"]
    scope: ["scripts/review.py", "scripts/improve.py", "scripts/tests/run_tests.py", "scripts/overlay.py"]
    title: "feedback receipt 권위를 가변 metadata cache에서 verbatim body 재파생으로 이전 — 읽기 경로가 body의 request-digest를 재파싱해 불변 sidecar generation에 resolve하고 stored metadata와 불일치 시 corrupt/pending 처리 (reply-metadata-json은 cache/진단 전용; 한 줄 편집 재귀속 차단) (JW-GPT-007)"
    status: done
    severity: "major"
    origin: "review-2026-07-18-generation-binding"
  - id: fix/pr-cycle-generation-binding
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-19-evidence-authority
    result: "@ a2ff310 (attempt-2, 위임 20260718T154107Z): cycle 증거 v2 승격(digest 필수)+명명 조회+skew 정직+신뢰 번호. live 804 green — JW-GPT-008 폐쇄"
    notes: "JW-GPT-008. [attempt-2] attempt-1(위임 20260718T151527Z, discard) 인수분: digest 결속·명명된 sidecar 조회·marker 단독 복구·legacy 비채택·RED 4종. 기각 2건: (1) [major] marker/freeze sidecar를 v1 schema 유지한 채 필드 추가 — 구 호스트가 재구성 시 digest 드롭 → 같은 cycle {None, digest} 위양성 conflict(review.py:534·improve.py:982)로 복구 차단. request binding v2 선례대로 schema 승격: digest 필수 v2, v1은 legacy 라벨 전용, 혼재는 skew 조건으로 정직 보고, (2) [minor] next_cycle_number(review.py:2019)가 latest_cycle을 operator 필터 없이 호출 — 신뢰 operator marker만 계상. tempdir 격리·env -u FORCE_COLOR."
    accept: ["PR freeze의 cycle 증거가 request generation을 명명한다 — waystone-review-cycle marker와 freeze sidecar 양쪽에 rendered_request_digest가 포함되고, 같은 cycle의 conflict 판정에도 digest가 참여한다", "improve와 ingest_round_binding의 PR 경로는 latest request가 아니라 cycle 증거가 명명한 digest의 불변 sidecar를 조회한다 — freeze되지 않은 B generation이 cycle-A의 provenance로 출력될 수 없다 (RED: freeze(A) 후 같은 target 재-prepare(B) → 투영이 A generation을 유지)", "로컬 freeze sidecar가 없어도 GitHub cycle marker 단독으로 exact generation을 복구할 수 있다 — marker의 digest로 sidecar를 resolve하고 부재 시 정직한 unknown", "legacy(digest 없는) cycle marker/freeze sidecar는 정직하게 구분된다 — 조용한 동등 취급 없이 pre-digest provenance로 라벨", "전체 테스트 스위트 green + ruff F401,F841 clean — freeze·merge gate·receipt 계약 무회귀", "freeze sidecar와 cycle marker는 새 schema 버전으로 승격된다 — digest 필수, v1 증거는 legacy 라벨 읽기 전용, v1/v2 혼재는 digest conflict가 아니라 정직한 버전-skew 조건으로 보고(위양성 conflict로 복구를 차단하지 않음) (RED: 신 marker + 구 형식 sidecar 혼재에서 복구 경로 생존)", "cycle 번호 산정은 신뢰 operator의 marker만 계상한다 — 비신뢰 commenter의 고번호 marker가 다음 freeze 번호를 강제하지 못한다 (RED)"]
    scope: ["scripts/review.py", "scripts/improve.py", "scripts/tests/run_tests.py"]
    title: "PR review cycle에 request generation 결속 — waystone-review-cycle marker·freeze sidecar·cycle conflict 판정에 rendered_request_digest 포함, improve·ingest_round_binding은 latest request가 아니라 marker가 명명한 digest의 sidecar를 조회 (cycle-A에 B provenance 귀속 차단; GitHub marker 단독으로 exact generation 복구 가능) (JW-GPT-008)"
    status: done
    severity: "major"
    origin: "review-2026-07-18-generation-binding"
  - id: fix/round-mint-anchor-immutable
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-19-evidence-authority
    result: "@ b3354c8 (attempt-1, 위임 20260718T134046Z): PROGRESS heading 분기 삭제 — exposure 단독 앵커. live 778 green"
    accept: ["round 기존성 판정은 유효한 immutable round exposure만 인정한다 — mutable PROGRESS.md heading은 기존성 증거에서 제거된다 (RED: exposure 없는 과거-dated round id를 PROGRESS heading만 추가한 상태로 close 시도 → 신규 mint로 취급되어 현재일 검증이 거부)", "정당 흐름 무회귀 — 기존 exposure가 있는 round의 익일 재close(확장)는 계속 허용된다", "전체 테스트 스위트 green + ruff F401,F841 clean"]
    scope: ["scripts/round.py", "scripts/tests/run_tests.py"]
    title: "round 기존성 판정에서 mutable PROGRESS.md heading 제거 — 유효한 immutable round exposure만 기존 round로 인정(또는 migration 시점 고정 allowlist), 임의 과거-dated round의 신규 mint로 v1 legacy 창을 여는 경로 차단 — '유한 전환일 잔여' 전제 복원 (JW-GPT-009)"
    status: done
    severity: "major"
    origin: "review-2026-07-18-generation-binding"
  - id: fix/probe-proof-principal-binding
    session_id: "513d1802-24e4-4adf-8ffb-a86b15e0f41d"
    round: 2026-07-19-evidence-authority
    result: "@ 69036c1 (attempt-2, 위임 20260718T140704Z): principal/config-root/process-context 축 + not-observed 상태 동등. live 783 green"
    notes: "JW-GPT-010. [attempt-2] attempt-1(위임 20260718T134047Z, discard) 인수분: principal(euid/gid/groups)·config root(present/not-present 정직 기록)·Linux process context 수집·fail-closed·기존 계약 무회귀. 기각 사유 1건: _codex_runner_reuse_blockers가 process 축 전부에 status==observed를 요구 — macOS는 전 축 not-observed라 marker가 영구 advisory(probe-once 파괴). 수정: 상태 동등 의미론 — 동일 not-observed는 match, 비대칭 전이만 재프로브(Linux 관측 저하 공격은 marker=observed라 비대칭 유지 — 안전). 컨테이너 namespace 축은 decision/trust-threat-model-boundary 위양 — 추가 금지. tempdir 격리·env -u FORCE_COLOR."
    accept: ["fingerprint에 실행 principal 축이 추가된다 — effective UID/GID·supplementary groups(portable), 획득 실패는 fail-closed (RED: principal 축만 다른 마커에서 프로브 실행)", "codex config root 정체성이 결속된다 — CODEX_HOME(기본 ~/.codex) resolve+stat identity, 부재는 정직하게 기록", "Linux best-effort process context를 기록한다 — /proc/self/status의 Seccomp·NoNewPrivs·CapEff, /proc/self/attr/current(존재 시); 관측 불가 축은 명시적 not-observed(조용한 생략 금지)", "기존 계약 무회귀 — exact-match 생략·축별 안내·resolved 경로 실행 통일·codex 부재 CI-동등 스위트 green 전부 유지", "전체 테스트 스위트 green + ruff F401,F841 clean", "not-observed 축은 상태 동등으로 대조된다 — marker와 현재가 동일 축에서 똑같이 not-observed면 재사용을 막지 않는다(macOS에서 probe-once 유지); observed↔not-observed 비대칭 전이는 재프로브 (RED: Darwin-형 전 축 not-observed 마커 재사용 성공 + 비대칭 전이 재프로브)"]
    scope: ["scripts/delegate.py", "scripts/tests/run_tests.py"]
    title: "probe fingerprint에 실행 principal·process context 결속 — effective UID/GID/groups, codex config root 정체성(CODEX_HOME/~/.codex resolve+stat), Linux best-effort process context(/proc/self/status의 Seccomp·NoNewPrivs·CapEff, /proc/self/attr/current) — 공유 checkout의 타 사용자/컨테이너 marker 적중 차단, 관측 불가 환경은 fail-toward-probe (JW-GPT-010)"
    status: done
    severity: "major"
    origin: "review-2026-07-18-generation-binding"
  - id: decision/trust-threat-model-boundary
    title: "신뢰 기계의 위협모델 경계 ruling — 리뷰가 3회전 연속 로컬-적대·다중사용자·프로세스 컨텍스트로 위협모델을 확장 중. SSOT §3(솔로 개발자·다중 머신)에 비춰 어디까지 하드닝이 비례적인가: 로컬 단일-파일 변조(007의 append-only/원격 store 변형), 컨테이너/namespace 축(010 심층), 이후 라운드의 수용 기준을 성문화"
    status: "pending"
    severity: "major"
    origin: "review-2026-07-18-generation-binding"
  - id: fix/pr-cycle-v1-supersession-honesty
    scope: ["scripts/review.py", "scripts/improve.py", "scripts/merge.py", "scripts/tests/run_tests.py"]
    accept: ["같은 cycle에서 최신 trusted marker가 v1이면 rendered_request_digest는 권위를 갖지 않는다 — classify는 digest 없이 skew 사유를 반환하고 exact generation 주장이 어떤 투영에도 나타나지 않는다 (RED: 늦은 v1 + 이른 v2 배치에서 v2 digest가 귀속되는 현행 동작 반전 — test_cycle_v1_v2_mix_is_version_skew_not_digest_conflict 기대 수정 포함)", "그 상태에서 merge gate는 PASS하지 않고 새 cycle의 v2 re-freeze를 요구한다 (fail-closed, RED)", "더 이른 v1 + 더 늦은 v2는 v2 digest 권위 유지 — 기존 동작 무회귀", "동일 ordering 규칙이 classify()·ingest_round_binding()·improve._review_binding() 세 표면에 동일 적용된다 (각 표면 RED)", "같은 timestamp의 v1/v2 tie는 fail-closed", "전체 테스트 스위트 green + ruff F401,F841 clean"]
    title: "same-cycle에서 v2보다 늦은 trusted v1 marker 존재 시 digest 권위를 unknown으로 강등하고 재freeze 요구 — 혼합 버전 PR cycle 오귀속(JW-GPT-011) 차단"
    status: "pending"
    severity: "major"
    origin: "review-2026-07-19-evidence-authority"
  - id: fix/freeze-sidecar-corruption-isolation
    scope: ["scripts/improve.py", "scripts/review.py", "scripts/tests/run_tests.py"]
    accept: ["freeze sidecar 파싱 실패는 request sidecar와 동형으로 corrupt sentinel 행으로 보존된다 — 파일명에서 round/cycle identity를 엄격 파생하고, 파생 불가한 파일은 격리 보고된다 (RED: 현행 warning-후-완전제외 동작 반전)", "최고 cycle 후보가 corrupt이면 해당 round의 binding은 honest-unknown으로 투영된다 — 이전 유효 cycle로 fallback해 explicit provenance를 얻지 않는다 (RED: 손상 fixture에서 stale cycle이 explicit로 승격되는 현행 동작 반전)", "content의 round/cycle이 filename identity와 불일치하면 corrupt로 취급된다 (RED)", "한 파일의 손상이 다른 round의 투영을 중단시키지 않는다 — 기존 격리 보존 (무회귀)", "전체 테스트 스위트 green + ruff F401,F841 clean"]
    title: "손상된 freeze sidecar를 corrupt sentinel로 보존 — 최신 cycle 손상 시 이전 cycle이 explicit 증거로 승격되는 fallback(JW-GPT-012) 폐쇄"
    status: "pending"
    severity: "major"
    origin: "review-2026-07-19-evidence-authority"
  - id: fix/probe-config-content-binding
    notes: "2026-07-19 실사고: 0.11.0 하네스에서 delegate run 발사 시 'runner preflight passed but fingerprint changed during the probe (codex_config_root)' — probe의 codex 실행이 ~/.codex에 세션/로그 파일을 생성해 디렉터리 stat이 변함 → 010 수정의 probe-중-변화 거부 로직이 자기 churn을 잡아 probe 영구 실패. 디렉터리 stat 축은 내용 변경(JW-GPT-013)엔 둔감하고 자기 churn엔 과민 — 동일 근원. 이 task 착지 전까지 이 머신의 delegate run 불가 → round-5는 raw codex exec 우회(사용자 규칙: 하네스 버그는 raw 우회 + finding 기록)"
    scope: ["scripts/delegate.py", "scripts/tests/run_tests.py"]
    accept: ["fingerprint가 codex가 실제로 읽는 config 입력(최소 CODEX_HOME 또는 ~/.codex의 config.toml)의 canonical content digest를 포함한다 — in-place 내용 변경 후 marker 재사용이 차단되고 재프로브된다 (RED: 내용 변경 후 현행 fingerprint 불변 입증 후 반전)", "config 파일 부재는 정직한 absent 상태로 기록되고 probe·재사용 양쪽 absent는 상태 동등으로 재사용 허용 — macOS probe-once 보존 (무회귀)", "존재하나 읽기 실패는 fail-toward-probe — 재사용 차단 (RED)", "marker에는 digest만 저장한다 — config 원문/내용은 저장 금지 (secret 포함 가능성)", "directory stat 축은 부가 진단으로 유지 (무회귀)", "전체 테스트 스위트 green + ruff F401,F841 clean", "probe 실행 자체가 유발하는 config root 디렉터리 변화(세션/로그 파일 생성 등 디렉터리 stat 변동)가 proof 기록·재사용을 막지 않는다 — 신뢰 대조 축은 probe 전후로 안정적인 config 내용 digest에 결속되고 디렉터리 stat은 진단 기록으로 강등된다 (RED: 2026-07-19 실사고 — 0.11.0에서 probe 중 codex_config_root fingerprint 변화로 기록 거부, delegate run 전면 불가)"]
    title: "probe proof fingerprint에 codex config 실효 내용 digest 결속 — config.toml in-place 변경 후 proof 재사용(JW-GPT-013) 차단"
    status: "pending"
    severity: "major"
    origin: "review-2026-07-19-evidence-authority"
