{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "decision",
    "decided_by",
    "criteria",
    "agent_checks",
    "warnings_seen",
    "rationale",
    "limitations",
    "at",
    "provenance",
    "verify_number",
    "profile_fingerprint"
  ],
  "properties": {
    "schema": {
      "const": "waystone-verdict-1"
    },
    "decision": {
      "enum": ["apply", "discard"]
    },
    "decided_by": {
      "enum": ["main-session", "user"]
    },
    "criteria": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["criterion", "met", "evidence"],
        "properties": {
          "criterion": {"type": "string"},
          "met": {"type": "boolean"},
          "evidence": {"type": "array", "items": {"type": "string"}}
        }
      }
    },
    "agent_checks": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["cmd", "exit", "summary"],
        "properties": {
          "cmd": {"type": "string", "minLength": 1},
          "exit": {"type": "integer"},
          "summary": {"type": "string", "minLength": 1}
        }
      }
    },
    "warnings_seen": {"type": "array", "items": {"type": "string"}},
    "rationale": {"type": "string"},
    "limitations": {"type": "array", "items": {"type": "string"}},
    "overrides": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["gate", "reason"],
        "allOf": [
          {
            "if": {"properties": {"gate": {"const": "blocker"}}},
            "then": {"required": ["verify_number", "finding_index", "refuted_by"]}
          },
          {
            "if": {"properties": {"gate": {"const": "unmet"}}},
            "then": {"required": ["criterion_indices"]}
          }
        ],
        "properties": {
          "gate": {"enum": ["blocker", "unmet"]},
          "reason": {"type": "string", "minLength": 1},
          "verify_number": {"type": "integer", "minimum": 1},
          "finding_index": {"type": "integer", "minimum": 0},
          "criterion_indices": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "integer", "minimum": 0}
          },
          "refuted_by": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "integer", "minimum": 0}
          }
        }
      }
    },
    "at": {"type": "string"},
    "provenance": {"const": "main-session"},
    "verify_number": {"type": ["integer", "null"], "minimum": 1},
    "profile_fingerprint": {"type": ["string", "null"]}
  }
}
